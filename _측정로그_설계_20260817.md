# 북스타 측정 로그 설계 v2 (관리자 5화면의 원천)

> 2026-08-17. 사장님 시안 `Desktop/북스타_관리자_5화면_2026.08.16.html`을 **요구사항**으로 받고,
> 그 화면들이 요구하는 숫자를 실제로 낼 수 있게 "무엇을 언제 어디서 기록할지"를 정한 문서.
> 6/3 `클레버/docs/measurement-design.md`(program_logs 단일표 설계)를 대체한다.
>
> 진행 순서: **1단계 로그 설계+테이블(이 문서) → 2단계 학생 앱 기록 심기 → 3단계 집계 함수 → 4단계 관리자 5화면 → 5단계 엑셀**

---

## 0. 한 장 요약

| 사장님 화면 용어 | 정의 (사장님 문서 그대로) | 원천 |
|---|---|---|
| **접속 학생** | 기간 안에 앱을 연 학생 수 (학번 중복 제거) | `bookstar_events` kind=`visit` |
| **조회** | 책 상세페이지를 살펴본 건수 | `bookstar_events` kind=`view` |
| **이용** | 종이책·전자책 = 도서관으로 **넘겨준** 건수(신청/대출), 고전 = 북스타에서 **읽은** 건수 | kind=`link`(종이·전자) + kind=`read`(고전) |
| **활동** | 한줄소감·한줄질문·서평 작성 건수 (최소 글자 통과, 숨김 제외, 퀴즈 제외) | kind=`activity` (voided=false) |
| **경로** (꼬리표) | 챌린지 때문에 생긴 이용인지, 상시 이용인지 | `program_id` null → 상시 / 값 있음 → 챌린지 |
| **유형** | 종이책·전자책·해외고전·국내고전 (외부참조는 화면에서 제외) | `item_type` |
| **출처** | 큐레이션·챌린지·검색·랭킹·피드… (운영이력 큐레이션 탭의 조회·이용) | `origin` + `origin_id` |

**설계 원칙**
1. **테이블 하나** (`bookstar_events`) — 사장님이 말한 "로그 1~6"은 이 표의 `kind` 값이다. 표를 6개로 쪼개면 집계마다 UNION 해야 하고 꼬리표가 어긋난다.
2. **기존 표는 건드리지 않는다.** 글 본문은 `bookstar_writings`/`reviews`, 별은 `bookstar_stars`, 퀴즈·완독율은 `bookstar_challenge_results`에 그대로. 이벤트 표는 "언제·누가·무엇을·어디서" 한 줄만 남기고 본문은 `ref_table/ref_id`로 가리킨다.
3. **추가 전용(append-only).** 학생 앱은 INSERT만 가능. 읽기·수정·삭제는 관리자 함수(service role)만. 8/15 잠금 로드맵의 ⓐ군(로그 5개)과 같은 리스크 등급.
4. **정직한 표기.** 종이책은 "대출"이 아니라 **신청까지**가 우리 기록이다(실제 대출은 퓨처누리). 화면 문구도 그렇게 쓴다.

---

## 1. 테이블 `bookstar_events`

| 컬럼 | 타입 | 뜻 | 예 |
|---|---|---|---|
| id | bigint PK | | |
| school_id | text | 학교 (지금은 앱 상수 `CH_SCHOOL`='hankuk' — 세명대 표기로 바꾸는 건 별도 정리) | hankuk |
| student_id | text | 학번 (`bxStudent().id`). 비로그인은 `guest` | 20240137 |
| session_id | text | 브라우저 세션 식별(탭 열 때 1회 생성, sessionStorage) | s_k3j2… |
| **kind** | text | `visit` `view` `read` `link` `activity` | view |
| sub | text | kind 세부. link: `pickup`(찾아줘북즈) `hold`(도서예약) `ebook_borrow` `crema_open` `opac_open` / activity: `oneline` `question` `review` / read: `session` | pickup |
| **item_type** | text | `paper` `ebook` `foreign` `korean` `external` `none` | paper |
| item_key | text | 종이책 `CATTOT…`, 전자책 brcd, 고전 `gb-…`/`kr-…`, 외부 isbn | CATTOT000123456 |
| item_title | text | 제목 (집계 화면에 그대로 씀, 조인 회피) | 불편한 편의점 |
| **origin** | text | 책을 만난 통로: `curation` `challenge` `search` `ranking` `feed` `classics` `detail`(상세→상세) `direct`(URL/복귀) `unknown` | curation |
| origin_id | text | curation=`library_sections.slot`, challenge=`library_programs.id`, search=검색어 요약 없음(빈값) | home-1 |
| **program_id** | text | 챌린지 맥락이면 그 챌린지 id (경로=챌린지). 아니면 null (경로=상시) | 0992d6f9-… |
| ref_table / ref_id | text | activity가 가리키는 본문 행 (`bookstar_writings`.id / `reviews`.id / `bookstar_challenge_results`) | bookstar_writings / 1043 |
| ok | bool | activity: 최소 글자 통과 여부(불통과도 기록은 남김, 집계에선 ok=true만) / link: 도서관 응답 성공 | true |
| voided | bool | 관리자가 글을 숨겨서 "안 쓴 것"이 됨 → 집계 제외 (다시 보이기면 false로) | false |
| seconds | int | read: 뷰어 열고 닫을 때까지 읽은 초 (하트비트 합계) | 412 |
| meta | jsonb | 부가 (read: read_pct, mode / link: 도서관 응답 코드 / view: 표지 유무 등) | {} |
| created_at | timestamptz | | |

인덱스: (school_id, created_at) · (school_id, kind, created_at) · (school_id, student_id, created_at) · (school_id, item_key) · (school_id, program_id) where program_id is not null · (ref_table, ref_id).

**정규화 규칙(앱 공통 함수 `bxEvent()`가 보장)**
- `item_type` 판정: `isCls(b)` → gb-=foreign / kr-=korean; 도서관 책은 `tags`→ isbn 모양(`sm-CATTOT`=paper, `sm-숫자`=ebook)→ 나머지 external.
- `origin`: 클릭 시점의 `data-origin` 조상 요소에서 읽는다(§3). 없으면 `unknown`.
- `program_id`: 그 책이 **학생이 참여 중인 챌린지에 담긴 책**이면(`chalForBook`) 그 id. 조회·읽기·활동 모두 같은 규칙.

---

## 2. 무엇을 언제 기록하나 (2단계에서 심을 지점 — 탐색 결과 기반, index.html 라인)

| kind | 언제 | 심는 위치 | 비고 |
|---|---|---|---|
| visit | 학생 확정 후 탭당 1회 (sessionStorage 플래그) | 부팅 SSO 수신 `~11277` + `bxStudent()` 복원 직후 | guest도 남기되 접속 학생 집계는 학번만 |
| view | 상세 열림 | `libDetail()` 6399, `openDetail()` 7633 진입부 | 같은 책 30초 내 재열림은 1건(중복 방지) |
| read | 뷰어 닫힘/이탈 | `closeViewer()` 9696 → `readerSessionEnd()` 3968, `pagehide` 6042 | seconds = 이번 세션 누적. **집계는 seconds ≥ 60만 "이용"** (탭 전환 잔여 제외) |
| link/pickup | 찾아줘북즈 성공 | `smReserve()` 5410 `d.ok` | ok=d.ok. 실패도 ok=false로 남김 |
| link/hold | 도서예약 성공 | `smHold()` 5481 | |
| link/ebook_borrow | 전자책 대출 성공 | `smEbookBorrowOpen()` 7075 `d.ok&&d.viewerUrl` | |
| link/crema_open | 크레마 링크 클릭 | `cremaHref()` 5607의 `<a>`에 onclick 추가 | 지금 훅 0개 |
| link/opac_open | 외부 OPAC 링크 클릭 | `libDetail` 6408/6470 `<a>`에 onclick 추가 | external 유형이라 화면 집계엔 안 나옴, 기록만 |
| activity | 글 제출 | 챌린지 글 `mpSubmitWrite()` 8729 writings insert 직후 / 상시 서평 `submitReview()` 10387 / 완독 소감 `certPromptSubmit()` 4520 / 장면 소감 `submitSceneImpression()` 9672 | ref로 본문 행 가리킴. 최소글자 미달은 앱이 막으니 ok=true가 기본, 자동 미인정 규칙(§4)에 걸리면 ok=false |

**퀴즈는 activity가 아니다** (사장님 정의). 챌린지 통계의 "미션 수행"은 `bookstar_stars`(quiz 포함)로 센다.

---

## 3. 출처(origin) 심는 방법 — 호출처 15곳을 하나하나 안 고침

- 렌더러가 만드는 컨테이너에 `data-origin="curation" data-origin-id="{slot}"` (챌린지 카드 목록: `challenge`/{program id}, 랭킹: `ranking`, 피드: `feed`, 고전 컬렉션: `classics`, 검색 결과: `search`).
- `document`에 capture 클릭 리스너 1개: `e.target.closest('[data-origin]')`를 읽어 `window._bxOrigin={origin, id, ts}` 저장.
- `libDetail`/`openDetail`이 `_bxOrigin`(3초 이내 것만)을 view 이벤트에 붙임. 상세→상세(연관도서)는 `detail`.
- 운영이력 "끝난 큐레이션 조회·이용" = origin=curation & origin_id=slot 로 집계.

---

## 4. 활동 인정·숨김 규칙 (사장님 문서에 없던 것 — 이번에 확정)

**자동 미인정 (ok=false, 별 없음, 사서 손 안 감)** — 학생 화면에 "이런 글은 인정되지 않아요"로 미리 안내
1. 최소 글자 미달 — 한줄소감 5 · 한줄질문 5 · 서평 100 (앱 `CH_MISSIONS`와 동일)
2. 같은 글자 반복 — 서로 다른 글자 수가 3종 이하 (ㅋㅋㅋ, ㅎㅎㅎ, …., 1111)
3. 연락처·링크 — 전화번호 / URL / 이메일 패턴 포함
4. 같은 학생이 같은 책에 같은 내용 재제출 (본문 완전 동일)

**사서 수동 숨김** — 그 외 욕설·비방·무관한 글. 관리자 [숨기기] = `bookstar_writings.hidden=true` **+ 이벤트 voided=true + 그 글의 별 회수**(`bookstar_stars`에 음수 행 1개, activity=`revoke:<원activity>`, ref 동일). [다시 보이기] = 반대로 원복(회수 행 삭제). 학생 화면엔 "숨김 처리된 글" 안내.

**별 체계는 바꾸지 않는다** — 6/21 확정 `bookstar_star_rates`(15종)·5등급 그대로. 이번에 새로 생기는 건 "회수 행"뿐.

---

## 5. 권한 (RLS)

- `bookstar_events`: RLS ON. anon **INSERT만** (WITH CHECK: kind ∈ 5종, school_id not null). SELECT/UPDATE/DELETE 정책 없음 → anon 못 읽음(개인 로그).
- 관리자 읽기·voided 갱신 = `admin-save` Edge Function(service role) 신규 op — 3단계에서 `stats_*` op로 집계 함수 호출.
- 별 회수·숨김 = admin-save `writings_patch` 확장(hidden 바뀔 때 events.voided + stars 회수 동시 처리).

## 6. 진행 현황 (2026-08-17 — 1~4단계 완료·배포)
| 단계 | 커밋 | 내용 | 검증 |
|---|---|---|---|
| 1 테이블 | efdf97d | `scripts/sql/bookstar_events.sql` DDL+RLS | anon INSERT 201 / anon SELECT 42501 / service role 조회 |
| 2 앱 기록 | 7a07e8e (260817a) | index.html `bxEvent` 모듈 + 훅 18곳 + 출처 위임(`.bx-org` display:contents 래퍼·정적 data-origin) + 자동 미인정 안내문 | 헤드리스 스모크: visit/view/read(3s)/activity 실적재·유형 판정 6케이스·규칙 4종·출처 slot 캡처 |
| 3 집계 | cf30d58 | `scripts/sql/bookstar_stats_fns.sql` — `bs_stats_overview/usage/challenges/challenge_detail/curation` + `bs_writing_hide`(글 hidden+이벤트 voided+별 회수/원복). anon 실행권한 없음, admin-save `stats_*`·`writings_hide` op(service role) 배포 | service role 호출 OK·anon 401·숨김 라운드트립(별 5→0→5) |
| 4 화면 | 1ef2451 | admin/index.html 대시보드·이용통계·**챌린지 통계(신설)**·학생 글·운영이력 교체 + SheetJS 엑셀(대시보드 4시트·이용통계 3시트·챌린지 요약/상세·학생글·운영이력) | 헤드리스 스모크(adminSave 스텁): 5화면 렌더·탭·숨김 흐름·엑셀 시트 구성, 예외 0 |

**미검증 1건**: admin-save `stats_*` op의 실호출은 관리자 비밀번호(ADMIN_SECRET)가 필요해 자동 테스트 못 함 → 관리자 로그인 후 대시보드가 숫자로 채워지면 OK(빈 기간이면 0). 실패 시 표에 "집계를 불러오지 못했어요 — stats stats_overview 401…" 식으로 원인 문구가 그대로 뜬다.

**시안 대비 다른 점(의도)**: ① 대시보드 차트 2개·프로그램별 표 제거(시안대로 담백) ② 학생 글 화면은 챌린지 글도 포함(상시 서평은 `reviews`에 학번 없이 저장돼 학번 화면에 못 올림 — 통합은 후속) ③ 챌린지 [결과 확정] 버튼 없음(시상 명단 동결=엑셀 보관, 시안 운영이력 안내문과 일치) ④ 운영이력 큐레이션 탭=칸별 이번 학기 실적(칸에 기간이 없어 '끝난 큐레이션' 불가) ⑤ 우수작 ☆ 버튼은 유지.

**8/17 보정(사용자 문구 검수)**: 앱의 완주(⭐30·enroll.done_at)는 **책 1권 단위**로 확인됨 → 집계의 완주를 "담긴 책 전부 × 켠 미션 전부"(`bs_ch_done_students`)로, 참가를 "1건이라도 활동"(`bs_ch_part_students`)으로 통일(`scripts/sql/bookstar_stats_fns_v2.sql`). 대시보드 참가/완주 칩도 같은 정의. 검증: 12권 챌린지 1권=참가1·완주0, 12권=완주1. 관리자 실로그인 집계 호출도 사용자 스크린샷으로 확인됨(운영중 D-44 표시). ⚠️앱 쪽 "챌린지 완주!" 축하·⭐30이 책 1권에 뜨는 것은 사장님 정의와 어긋남 — 후속 결정 필요.

**후속**: ~~관리자 실로그인 검증~~ → 학생 앱 실사용 데이터 확인(테스트 계정 `book` 행이 별 순위에 보임 — 사장님 시연 전 정리 여부 결정) → `CH_SCHOOL='hankuk'` 표기 정리 → 상시 서평 학번 저장.

## 7. 정리해야 할 것 (별도)
- `CH_SCHOOL='hankuk'` → 세명대 식별자 통일 (별·글·이벤트 전부 같은 값이라 지금 당장은 집계에 문제 없음)
- 상시 서평이 `reviews`, 챌린지 글이 `bookstar_writings`로 갈라져 있음 → 이벤트가 둘 다 가리키므로 집계는 문제 없음. 본문 통합은 나중.
