# 세명대 공식 데이터 이관 계획 (2026-08-06 수립)

> 목표: 스크래핑으로 모은 종이책·전자책 데이터를 전부 걷어내고,
> 퓨처누리 TulipWeb2 공식 API(lib.semyung.ac.kr/openapi) 데이터로 교체한다.
> 원칙: **새 데이터 완성·검증 → 전환 → 그다음 삭제** (삭제 먼저 금지)

## ▶ 진행 현황 (2026-08-06)
- ✅ **P0 완료**: 테이블 2개 생성(semyung_tulip, semyung_sync_state), 수집 스크립트 `scripts/tulip_sync.py`, 소량 테스트
- ✅ **P1 완료**: 전수 318,993건 적재·검증. 신착 기준점 버그(130712형 ctrl) 수정. 신착 로직 실동작 확인(555035 기준)
- ⬜ **P2 다음**: 전자책 표지 `python scripts/tulip_sync.py --enrich-ebook`(약 2h) → 종이책 알라딘 lazy 표지 배선
- ⬜ P3 별이·앱 전환 → ⬜ P4 구테이블 삭제
- **다음 세션 시작**: "세명대 연동 이어서 — 이관계획 P2부터"
- **병렬 진행**: 퓨처누리 메일(바탕화면 초안, 질문 2개) 발송은 사장님 몫

## 실측 확정 사실 (2026-08-06, probe 22회)
- 전체 장서 318,997건 (종이책 ~29.5만 + 전자책 23,496)
- apikey 없이 응답. display=1000 실동작 → 전수 319회 요청
- ISBN 99%(종이책) / 제어번호(CTRL)가 등록 순번 → 신착 = 최대 CTRL 위로 증분
- 최대 CTRL 555035 (등록 2026-07-29). 평상시 입고 월 400~500권
- 전자책 뷰어링크(uri_856) 100%, 교보 표지 = barcode로 조립 성공, YES24 표지는 404

## 1. DB — 새 테이블 3개 (Supabase gkujptyfrzqrjrvovbnc)

### semyung_tulip (서지 마스터, ~32만 행)
| 컬럼 | 내용 |
|---|---|
| ctrl (PK) | 제어번호 12자리 |
| kind | paper / ebook (소장위치로 판별) |
| title, author, publisher, pub_year | 검색API DISP01/02/03/06 |
| isbn | DISP08 (종이책 99%) |
| call_no, class_no | 청구기호·분류(DDC) |
| lang, mat_type | 언어·자료유형(LIMT03/01) |
| reg_date | 등록일 DISP09 (신착 정렬 키) |
| vendor, barcode, viewer_url | 전자책 전용 (bookinfo uri_856에서) |
| cover_url | 표지 (아래 3절 규칙으로 채움) |
| search_text | FTS용 결합 텍스트 |

- 임베딩 컬럼은 **일단 없음** — 32만×1536차원 ≈ 2GB라 스토리지 초과 위험.
  검색은 FTS(한국어 형태소 없이 trigram)로 시작, 벡터는 전자책 2.4만 등 부분만 추후.
- RLS: SELECT는 공개(서지는 공개정보), 쓰기는 service role만.
  (semyung_catalog RLS OFF 사고 재발 방지)

### semyung_sync_state (1행)
last_max_ctrl, last_run_at, last_result — 신착 증분의 기준점.

### 기존 테이블 처리 (P4에서, 검증 후)
- semyung_catalog (스크래핑 장서, count 타임아웃 날 만큼 큼) → 삭제
- semyung_books (전자책 미러 23,727, 임베딩·표지URL 보유) → **표지·임베딩 재활용 후** 삭제
- semyung_new / semyung_loan_rank → 새 파이프라인으로 대체 후 삭제

## 2. 수집

### 초기 전수 (승인 회신 후 실행)
1. 검색API alpha&query=TOTAL, display=1000 × 319페이지 → 기본 서지 32만 건 (약 30분)
2. 전자책 23,496건만 bookinfo 추가 호출 → barcode·viewer_url (0.3초 간격, 약 2.5시간)
3. 적재 후 검증: 건수=318,997 대조, 표본 50건 도서관 홈페이지와 눈 대조
- 요청 간격 0.5초 이상, User-Agent 명시. 실패 페이지는 기록 후 재시도

### 신착 증분 (매일)
- 방식: last_max_ctrl+1부터 bookinfo 순회, **연속 30개 빈 번호면 종료** (빈 번호 2.5% 감안)
- 새 레코드는 서지 upsert + 전자책이면 barcode 추출 + last_max_ctrl 갱신
- 시각: **매일 03:30** (사서 등록업무 종료 후, 휙 네이버수집 03:00과 어긋나게)
- 실행 위치: **한국 로컬(사장님 PC) 작업 스케줄러** = BookstarTulipSync
  - GitHub Actions는 해외 IP 차단 가능성 큼(현 스크래핑이 그래서 관장님 PC로 도피 중)
  - P0에서 Actions 1회 호출 테스트 → 통과하면 Actions로 승격 검토
  - .bat는 %~dp0 사용 (한글경로 침묵실패 교훈)
- 소장·대출상태는 저장 안 함 — 화면 열 때 bookinfo 라이브 조회(지금 semyung-holding 방식 유지, 소스만 openapi로)

## 3. 표지

| 대상 | 방법 | 근거 |
|---|---|---|
| 전자책·교보 | ebook.semyung.ac.kr/upload/20213/content/ebook/{brcd}/L{brcd}.jpg 조립 | 실측 9/9 성공 |
| 전자책·YES24 | ①구 semyung_books의 cover URL을 barcode/제목 매칭으로 이식 ②실패분 알라딘(제목+저자 엄격매칭) ③그래도 없으면 기본 타이포 표지 | 구 테이블에 YES24 표지 실존 확인 |
| 종이책 | **지연 조달**: 화면에 노출될 때 알라딘 ISBN 직조회 → cover_url에 캐시. 신착·베스트만 선배치 | 알라딘 일 호출한도(수천 회)로 32만 전수 배치 불가. ISBN 직조회라 오매칭 없음 |

- placeholder 판정은 **GIF8 매직바이트만** (크기 기준 금지 — 「고래」 6.4KB 실표지 교훈)
- 제목 매칭 시 **저자 성 일치 필수** (8/6 종이책 대조에서 오매칭 70% 실증)

## 4. 앱·별이 전환 (P3)
- 별이 도서검색 → semyung_tulip FTS로 교체 (전자책 임베딩은 새 테이블 기준 재생성 ~2.4만)
- semyung-best/new(신착·베스트 Edge Fn) → semyung_tulip 기반으로 재작성
- semyung-holding → openapi bookinfo로 소스 교체 (스크래핑 제거)
- 예약(semyung-reserve, 찾아줘북즈)은 **현행 유지** — 퓨처누리 예약 API 회신 오면 교체

## 5. 진행 순서 (2026-08-06 사장님 결정 반영)
- **승인 대기 없음**: 규격서를 보내줬다 = 쓰라고 준 것. 수집 즉시 시작.
  메일은 별도 안건(apikey 정책·MYL-001·uid 확인)으로 발송하되 작업을 막지 않음
- **구 데이터 이식 없음**: 표지·임베딩도 전부 새로 만든다 (임베딩 재생성 비용 1달러 미만).
  YES24 표지는 알라딘 엄격매칭 + 기본 타이포 표지로 신규 구축
- **P0**: 테이블 DDL + 수집 스크립트 + 1,000건 소량 테스트
- **P1**: 전수 수집·적재·검증 (반나절)
- **P2**: 표지 채움
- **P3**: 앱·별이 전환 → 동작 확인 → **당일 구 테이블·스크래핑 스크립트 삭제**
  (이용자 0 데모 + API 30분 재수집이 곧 백업. 유일한 확인: 별이가 새 테이블을 보는지)

## 예약·연장 구분 (혼동 주의)
- **연장(renew)**: openapi myloan에 있음 → 정식 API로 구현 가능
- **예약(신청)**: openapi에 **없음** (가능여부 표시만) → 찾아줘북즈는 당분간 현행(스크래핑) 유지,
  메일 회신으로 예약 API 규격서 오면 교체

## 리스크
- Actions 해외 IP 차단 → P0에서 5분 테스트로 확정, 막히면 로컬 확정
- 서버가 무료 개방을 잠글 가능성(apikey 강제 전환) → 메일에 apikey 발급 요청 이미 포함, 코드에 apikey 파라미터 자리 미리 마련
- semyung_catalog가 어떤 화면에서 아직 쓰이는지 P3 전에 grep 전수 확인
