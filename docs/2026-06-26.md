# 2026-06-26 (klever_demo / 북스타) 작업 기록

## 1. 크레마클럽(YES24 구독) 연동 — C(딥링크)로 확정·배포 (524fb82)
### 실측으로 확정한 근본 제약 (더 팔 거 없음)
- **크레마클럽은 웹 리더가 없음.** 독서는 YES24 eBook **PC설치뷰어(.exe)/모바일앱/e-ink**에서만. 웹은 검색·담기까지. → 교보 구매책(웹뷰어)처럼 "북스타 안에서 읽기" 구조적 불가.
- 데스크톱 뷰어 스킴 `yes24ebookcremaclub://downloadbook/?id=..&ebookcode=..&open=true&auth_token=<30일JWT>` 존재. 빈 스킴 `yes24ebookcremaclub://`은 뷰어를 깨끗이 엶.
- 하지만 **id/ebookcode가 책 비특정**(서버상태로 책 선택) + 안 담긴 책 쏘면 **뷰어 크래시**. auth_token=tlo1020 30일 JWT(HS256, 우리가 못 만듦).
- 레지스트리 가로채기로 실측: HKLM `YES24eBookCremaClub` 스킴, 뷰어=`C:\Program Files (x86)\YES24eBook\`. DRM=ezPDFBook+Widevine. 계정 tlo1020(세명대 크레마 ~2027-01).
- → **스킴/토큰 방식은 데모 1계정 한정이라 폐기.**
### 채택: C(딥링크) — 일반화 가능·무인증
- 북스타 크레마 책 클릭 → `cremaclub.yes24.com/BookClub/Detail/{goodsNo}` 새 탭 직행 → 학생 본인 YES24 세션에서 바로읽기/담기 → 본인 뷰어로 읽기.
- `app.html` `smCremaOpen` 제거 → `cremaHref(cremaUrl,title)`: Detail URL 있으면 그 책, 없으면 제목검색 폴백. [app.html:4390~]
- **크레마 goodsNo = YES24 전자책 상품번호** 확인. 매핑: 알라딘(ISBN→제목)→크레마 `Search?query=제목`→Detail링크(서버응답에 그대로, 로그인불필 Mozilla UA). semyung_enrich.json crema 11권 Detail URL og:title 대조 **10/10 일치**. ⚠️크레마 ISBN검색 안 됨.

## 2. 좌측 네비 애니메이션
- 첫 등장: 화면 중앙→좌측 도킹(최초 1회, PC). 스크롤 드리프트(`.main` 스크롤 기준, 최대 28px). [app.html playSideNavEntrance/initSideNavFloat]
- ⚠️ 부팅 시 `nav('ourlib')`가 [app.html:8316]에서 호출되어 등장연출 켜짐. 안 보이면 캐시(Ctrl+Shift+R).

## 3. 우리 학교 대출 랭킹 — 대출횟수·▲▼ 제거 (3d8d1a1)
- `mlRank`에서 `loanMove`(▲▼/NEW)·`대출 N회` 제거, 범례 "지난달 대비 ▲▼"→"최근 6개월". 순위번호+책만.
- 데이터 출처: 종이책=OPAC `statistics/popularloanList?category=1,2,3`(게임/비도서 제외)→`build_semyung_loan_rank.py`→Supabase `semyung_loan_rank`(표지=알라딘, prev_rank=우리 계산). 전자책=`semyung-best` Edge Fn(`ebook.semyung.ac.kr/elibrary-front/main/bestContent.json?bestCttsDvsnCode=1`). 둘 다 세명대 실데이터, 우리는 가공/통합.

## 4. 표지 안전망 (6be06d4)
- 문제: 세명대 "이미지 준비중" placeholder = **.jpg 주소로 위장한 GIF** → 브라우저엔 정상이미지로 보여 **런타임 자동수정 불가**.
- `scripts/build_cover_overrides.py`: 세명대 큐레이션 책 표지 검사→GIF면 알라딘 실표지(제목+저자 매칭 가드)→`books/cover_overrides.json`. 앱 `rebuildLibPool`에서 brcd 매칭 덮어쓰기.
- ⚠️교훈: **GIF만 placeholder 신호**. 크기<8KB 금지(고래=6.4KB JPEG 실표지 오판). 접근실패는 교체안함(세명대 차단 시 전체 오교체 방지).
- 수정: 용의자X(GIF placeholder)·프로젝트헤일메리(cover 필드 없었음) = 알라딘 실표지. 고래·미비포유는 실표지라 그대로.

## 5. 형태 태그 정확도 — ⚠️ 미해결(다음 세션)
- 태그 출처 = `semyung_enrich.json`(brcd), `fmtTags()`[app.html:4483]. 전자책=`b.lib`, 종이책=`en.paper`, 구독=`en.crema`.
- 앱 렌더링은 정확(버그 없음). 전자책 태그=항상 정확. 구독 표시된 건 정확(잘못 붙은 것 없음).
- **문제 = 누락**: ① "내가 돈을 벌고 있다는 착각" 실제 크레마 구독인데 태그 없음 ② "살인자의 기억법"은 enrich에 아예 없어 전자책만 뜸.
- 근본원인: enrich=주기적 빌드인데 라이브 베스트(매일 변동)에 미enrich 책 섞임.
- 검증 함정(다음 세션 주의): 크레마 제목검색=동명 비평서 오탐(저자정밀매칭 필요), 종이책 OPAC=아무 CATTOT나 잡힘(자료유형 실파싱 필요, 302 세션쿠키).
- 다음 할 일: enrich를 라이브 베스트 전체 대상으로 정밀 재빌드.
