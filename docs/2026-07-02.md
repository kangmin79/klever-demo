# 2026-07-02 — 해외고전 챕터블록 렌더링 완성 (gb-6593·gb-135 + 14권 헤더/누출)

> 이 스레드 핸드오프 = 메모리 `project_bookstar_foreign_render_lint_plan`.
> 시작 한 줄: **"project_bookstar_foreign_render_lint_plan 보고 이어서"**
> ⚠️ klever repo(cd klever_demo, kangmin79/klever-demo), 한글=Edit/Write. DB키=service_role SR in `C:/tmp/align_koonly.mjs`, 프로젝트 gkujptyfrzqrjrvovbnc. body_sent는 DB라 git 불필요(코드=app.html만 배포).

## 사용자 요청
"여러 에이전트로 한번에 다 처리" + "레미제라블 비싸게 번역했는데 왜 어긋나냐(재번역 아님, 정렬만 문제)" + "정말 문제없게". gb-135는 **챕터 단위까지** 가라고 결정.

## 완료 (전부 라이브)
### 1. 챕터블록 14권 헤더 한국어화
- `apply_koheader.mjs --write` gb-121·1184·164·2226·4217·564·821·5921·913·28054·580·2701·74·76 → 헤더셀 ko `# 제N장`(카라마조프 부/권/장). 멱등·쓰레기0.
- ⚠️ apply_koheader 백업 버그(재fetch 주석만, 실제 백업 안함) → `_backup14.mjs`로 별도 백업(koblock_backup/).

### 2. 헤더 누출(산문에 박힌 `# 제N장` 리터럴) 전수 정리 = 26셀
- scan_header_leak가 샘플만 보고 → **광범위 스캐너 `_leakall14.mjs`로 실측**: gb-1184 8·gb-821 14·gb-2701 2·gb-564 1·gb-913 1.
- 리더 `sentBlockHtml`(app.html:6150) = 산문셀 esc만 → `# 제N장` 리터럴 노출, 챕터경계는 isHeadPara셀만. 해당 챕터 standalone 헤더 존재 셀별 확인 후 정확제거(prefix/suffix/mid/exact/empty). 산문 손실 0. 백업 leak_backup/.
- 스크립트: `_fixleak6.mjs`(gb-1184/2701/564/913 초기6)·`_fix1184.mjs`(gb-1184 추가6)·`_fix821.mjs`(14). **14권 누출 0** 재확인.

### 3. gb-6593 톰존스 재구축 완성
- 데이터: `body`(영어 199만자 온전)+`body_trans`(한국어 105만자 온전). body_sent 오정렬이라 재조립.
- 에이전트가 "완료" 보고 → **독립검증(_verify6593/_merge6593)으로 41챕터 한국어 빈칸 결함 발견**(다음챕터 ko가 직전으로 병합, 직전 ko/en비 0.78~1.15 vs 정상0.70)+Book1Ch1 헌사 오배치.
- 에이전트 재개(SendMessage)로 **내용앵커링** 전량수정 → 207챕터 빈ko 0, 415그룹. 샘플(B1Ch1 bill-of-fare·B1Ch5·B18Ch12) 좌우완벽. 백업 realign_backup/gb-6593.json.

### 4. gb-135 레미제라블 챕터단위(365장) 완성 — 핵심 성과
- 데이터: `body`(영어 327만자 온전, TOC 앞20030자)+`body_sent` ko칸(진짜번역 순서보존)+`body_trans`=null.
- 1차: 권단위 정렬 + **헤더버그 수정**(en "BOOK FIRST"가 chPat 실패 → `# ` 프리픽스로 mdPat 통과, `_fixhdr135.mjs`, 53섹션). ko/en비 0.50 일관.
- 2차: **5 병렬에이전트(부1~5)** — `_prep135.mjs`로 부별 ko셀(ko_vol*.json)+영어(en_vol*.txt) 추출 → 각 에이전트 챕터 내용앵커링 → out_vol{N}.json(DB 안씀).
- ⚠️ **에이전트 문단그룹화 제각각**(vol1 챕터헤더가 152셀그룹에 묻힘→isHeadPara실패, vol3 챕터당 1거대문단, vol5 셀당1그룹 18k+"#제N권"마커 8개 ko누출, vol4 ~2그룹).
- **`_assemble135.mjs`로 통일 정규화**: 평탄화 + 헤더셀 감지(경계) + 누출마커 스킵 + en줄은 **문장끝에서만** 문단화(340자) + ko문장 문단화(230자). **en/ko 무손실 TRUE**(3.17M/1.64M자 in==out). → 11,770그룹, 헤더419(부5·권48·장365·서문1), 빈챕터0, 누출0.
- `_write135.mjs`로 PATCH 204. `_live135.mjs` 라이브 검증 통과. 백업: realign_backup/gb-135.json(원본)·gb-135_prehdr.json·gb-135_booklevel.json(권-level 35042).

### 5. 배포
- **commit 8903aa1**: CHAPTER_BLOCK_BOOKS에 gb-6593·gb-135 추가(app.html:6141 1줄). 챕터블록 **총 16권**. app.html만 명시 커밋(다른 WIP파일 미포함).

## 검사기 상태
`classics_render_lint.mjs`: 143권 / koleak 0. gb-6593·gb-135는 블록모드라 checker 버킷은 NEED_REALIGN이어도 정상(문장쌍 비교는 블록모드에 무의미).

## 🔜 다음 (순서)
1. **사용자 육안확인** — 리더에서 gb-6593 톰존스·gb-135 레미제라블 좌우 챕터정렬·문단 확인 (GitHub Pages 캐시 10분, 시크릿창/Ctrl+Shift+R). 문제시 백업 롤백.
2. **Phase1 KO2COL 8권** — body_sent null+body_trans인 8권을 리더에서 한국어 1단 기본+영어 토글로. app.html viewerBodies/renderViewer 코드변경(별개, 1권먼저→육안→배포).
3. **Phase6 권구분 확장** (다권책 챕터 리셋 지점 권헤더) / **Phase7 게이트 최종**.

## 스크립트 (C:/tmp, 지속)
apply_koheader·header_ko·classics_render_lint·scan_header_leak(기존) + 신규: _backup14·_leakall14·_fixleak6·_fix1184·_fix821·_verify6593·_merge6593·_fixhdr135·_prep135·_valvol·_assemble135·_write135·_live135·_sample135·_gran·_struct. gb135_share/(ko_vol*·en_vol*·out_vol*·assembled.json).
