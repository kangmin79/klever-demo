# 세명대 계약(8월 중순) 전 준비 계획

*작성: 2026-07-10 / 외부 대기: SSO 로그인 규격 · 종이책/전자책 도서 데이터*

## 배경
- 세명대 데모 종료 → 8월 중순 정식 계약 예정
- 대기 중인 외부 입력 2개: ① SSO 로그인 방식 ② 도서 데이터(종이책/전자책)
- 이 둘 없이 미리 할 수 있는 것을 먼저 완성한다

## 핵심 발견 (2026-07-10 실측)
1. **보안**: bookstar_* 16개 테이블 전부 RLS 정책이 `ALL/true/true` = anon 무제한 읽기·쓰기·삭제.
   인증 자체가 없음(localStorage 이름표 하나로 신분 결정). 점수도 클라 계산 → 서버 무검증 저장.
   → **근본 차단은 SSO 필요**(서버가 학생 신원 검증 가능해야 RLS를 "본인 것만"으로 조임).
   지금 실학생 0 → 위험 0. **8월 학생 유입 전 잠그면 됨.**
2. **도서 파이프라인은 이미 존재**: `semyung_new` 테이블 + `_semyung_sync.bat`(build_semyung_new*.py).
   신규 구축 불필요. 세명대 데이터 오면 형식 맞춰 기존 파이프라인에 upsert.

## 작업 순서

### A. 사서 대시보드 (지금 최우선) — 진행 중
- 목적: 전교생 완독율·퀴즈 정답률·독서량을 사서가 한눈에. 계약 미팅 데모.
- 재활용: `renderLeaderList`(app.html:8594)의 전체학생 4쿼리 + `_myStats`(7631) 집계규칙 + `.my-stats` 타일 CSS
- 데이터: bookstar_challenge_results(read_pct·read_sec·quiz_ok·score) + bookstar_students + bookstar_reader_stats
- 진입: `?page=dashboard` 딥링크(사서 전용, GNB 미노출) — nav()(4536)에 분기 추가

### B. SSO 어댑터 준비
- 식별 로직 3함수로 캡슐화 확인됨: bxStudent(5250)·bxSetStudent(5251)·_bxSid(5252)
- SSO 오면 이 3곳 내부만 교체 → 소비자 40여곳 무변경
- 게스트 기록 승계: guest 로컬 캐시(_chalKey)를 로그인 시 서버로 이관하는 함수 신설
- 로그인 성공 훅 자리 마련(현 bxPickStudent 5354의 후처리 재사용)

### C. 보안 리포트 + 잠금 SQL 계획서
- 위조 가능 지점 문서화(이 파일 하단)
- SSO 연동 시 실행할 RLS 잠금 SQL 초안 준비(본인 행만 쓰기, 점수는 서버 RPC 재계산)
- 실제 적용은 SSO 인증 방식 확정 후

### D. 도서 데이터 도착 체크리스트
- 대상 테이블: public.semyung_new (ref gkujptyfrzqrjrvovbnc), on_conflict=brcd 멱등 upsert
- 필수 필드: brcd, isbn13, title, author, publisher, pub_year, pub_date, cover, store, detail, summary, kind
- 실행: _semyung_sync.bat (로컬 실행 — 세명대가 해외 IP 차단)
- 받으면 할 일: 형식 매핑 → build 스크립트 조정 → 표지/줄거리 보강(enrich_semyung.mjs) → upsert → 검증

## 보안 위조 지점 상세 (발견 1 근거)
- 인증 부재: app.html:5250-5252, 5354-5356 — localStorage 값 하나, 비번 없음, 임의 student_id 사칭
- 점수 클라 계산: 5274 `score=ok*10+submitted*50` → 5278 그대로 upsert
- 완독율 클라 전송: 3764 → 5263 (read_pct/read_sec 임의값 가능)
- anon 키 하드코딩: 4920(COVER_ANON), curl 직접 호출 가능
- RLS 실측: 전 테이블 `ALL/qual:true/with_check:true` (anon,authenticated,public)
