# 세명대 도서관 연동 — 2026-08-08 작업 리뷰

새 세션에서 이 문서부터 읽으면 됩니다. 커밋 12개(`a2e6584..b2050f4`), 파일 10개, +1,342줄.

---

## 한 줄 요약

**"북스타 안에서 종이책·전자책을 빌리고 예약하는 전 과정"이 코드상 완성됐다.**
남은 것은 ① 퓨처누리 배너 요청 메일 발송 ② 실학생 검증 ③ 알림(B단계).

---

## 1. 이 세션의 결정적 발견 3가지

### ① openapi의 uid는 학번이 아니라 liid다 🔑🔑
- `myloan`·`myreserve`에 `uid=학번`을 넣으면 **영구히** "해당 이용자 정보가 없습니다"
- 진짜 uid = **liid**(도서관 내부 회원번호, 13자리 예 `0000020149410`)
- liid는 **lib 세션의 `/relation/eBook` 페이지에서만** 얻을 수 있다
- **판별법**: 존재하는 uid면 "현재 대출불가인 이용자입니다"처럼 이용자별 상태가 옴.
  없는 uid는 "해당 이용자 정보가 없습니다" 고정
- 도서관 API 스스로 확인해 줌 — `loanreq` 응답의 `user_id`가 liid로 찍힌다
- **함의**: 배너가 학번만 주면 종이책 개인기능조차 영영 불가능. 이게 진짜 병목이었다

### ② school_no + portal_user_id 두 값 = 비번 없는 만능 열쇠 🔑🔑
- 포털이 발급한 이 두 값만 있으면 **비밀번호 없이, 포털 쿠키 없는 완전 별개 서버에서**
  lib 개인세션 생성 가능. 30초 뒤 재사용도 성공(1회용 아님)
- → 학생 비번을 안 받고도 ①종이책 대출현황·연장·예약 ②전자책 본인명의 대출 **둘 다** 해결
- **퓨처누리에 요청할 것은 딱 이 2필드**

### ③ 미문서 엔드포인트 2개 발견
- **`/openapi/loanreq`** = 찾아줘북즈(서가픽업). list/request/cancel 전부 실동작
  - ⚠️ 철자 혼합: `controlno`(언더바 없음) + `accession_no`·`main_no`(있음). 통일하면 011
- **`/openapi/mynotice`** = 개인공지
- 규격서 미수령 → 메일에 문서 요청 포함

---

## 2. 만든 것

### Edge Functions
| 함수 | 역할 |
|---|---|
| `_shared/semyung_session.ts` | lib→/relation/eBook→mmbrLnkg 체인 공용화 |
| `_shared/sso_token.ts` | HMAC-SHA256 서명 세션토큰(시크릿 `SSO_TOKEN_SECRET`) |
| `_shared/sso_store.ts` | `sso_sessions` 표 접근. liid·포털값은 여기에만 |
| `semyung-my` (신규) | myloan / myreserve / mypreserve / **loanreq** / mynotice 프록시 |
| `sso-login` v2 | 연계값 받으면 liid 확보·저장 → `sso_personal=1` |
| `semyung-ebook-borrow` v2 | 개인세션 대출·반납·연장·예약·재고. 공유계정 폴백 |

### DB
- **`sso_sessions`** (RLS 잠금, service_role 전용) — sid, hakbun, liid, school_no, portal_user_id, expires_at

### 앱 화면 (app.html)
- **사이드바 "내 도서관"**(6번째, library 아이콘) — 도서관 계정 연결된 학생에게만 노출
  - 이용 상태(연체·대출중·기다리는중) + 빌린 책(종이·전자 통합) + 기다리는 책
  - **아이콘 배지**: 연체(빨강) > 예약도착(초록) > 반납임박(주황)
- **우리 도서관 상단** 한 줄 요약 + `내 도서관 ›` 링크
- 책 상세: 전자책 **실시간 재고**("지금 대출 가능"/"대출 중" + 예약 버튼·대기인원)
- 전자책·종이책 동시 보유 도서도 **종이책 소장정보를 펼쳐서** 표시

---

## 3. 검증된 것 / 안 된 것 (⚠️ 리뷰 시 여기를 봐야 함)

### ✅ 실데이터로 가역 검증 완료
| 기능 | 검증 내용 |
|---|---|
| 전자책 대출·반납 | brwCnt 0→1→0, 재고 0/1→1/1→0/1 |
| 전자책 연장 | `contentExtendProc` ok |
| **전자책 예약** | 예약 0명 → 신청 → **1명** → 취소 → 0명 |
| 찾아줘북즈 | 신청→현황→취소 3회, 잔여 0건 |
| SSO 토큰 | 무토큰 401, 게스트 폴백 정상 |
| 화면 렌더 | 보조함수 14케이스 + 통합 6케이스 + 내도서관 6케이스 |

### ❌ 아직 못 본 것 — 실학생 계정 필요
- **종이책 대출목록·연장** — 테스트 계정 대출 0권이라 규격서 예제로만 검증
- **종이책 예약(myreserve)** — 테스트 계정 `book`이 **"현재 대출불가인 이용자"**
  - 도서관 웹에서 눌러도 동일(사장님 직접 확인) → 우리 코드 문제 아님. 계정 등급 문제
  - 찾아줘북즈는 정상 → 예약만 대출자격을 요구하는 것으로 보임

---

## 4. 잡은 버그 (전부 "화면은 멀쩡한데 죽어 있던" 유형)

1. **전자책 반납 버튼이 계속 400 실패** — 서버가 반납에도 brcd를 요구했는데 앱은 안 보냄
2. **전자책 예약이 조용히 실패 3연타**
   - 버튼이 안 뜸: `SVG_EBOOK`이 다른 함수의 지역 상수 → ReferenceError를 catch가 삼킴
   - 신청 실패: `dvsnCode:"W"` 누락
   - 취소 실패: 바코드가 아니라 **예약번호(prenSrmb)** 필요
3. **이용 상태 숫자 모순** — "0권 대출 중"인데 아래엔 2권(info API가 종이책만 셈)
4. **저자·줄거리가 통째로 빔** — 보강 로직이 `b._sm` 플래그에 의존, 일부 목록은 안 실음
5. **바코드 오추출** — 반납버튼 첫 인자는 도서관코드(20213). 자릿수로 훑으면 페이지
   머리말의 JS 캐시숫자(13자리)를 집음 → `fnContentClick` 패턴으로 정확 추출
6. **myreserve `location` 필수**(규격서 미기재), **mypreserve 철자는 예제가 정답**

> 교훈: 오류를 삼키는 try/catch 때문에 로그에도 안 남았다. 화면이 멀쩡해 보여도
> 실제로 눌러 상태가 바뀌는지 확인해야 한다.

---

## 5. 지금 테스트하는 법

1. `bookstar.co.kr/_sso_test.html` 접속
2. **아래쪽 초록 박스**의 〔개인기능까지 열고 로그인〕 — 아이디 `book`은 기입돼 있고,
   비밀번호(`semyung7002`)는 **직접 입력** (공개 페이지라 8/8 리뷰에서 기입값 제거)
3. 우리 도서관 → 사이드바에 **내 도서관** 아이콘 생김
4. 책 상세에서 찾아줘북즈 신청 → 내 도서관 "기다리는 책"에 표시 → 취소

⚠️ **신청·예약 테스트는 도서관 직원이 실물을 꺼내는 실세계 작업.** 반드시 즉시 취소하고
잔여 0건 확인할 것. 전자책 대출도 반납까지 하고 끝낼 것.

---

## 6. 다음 할 일

1. **⭐메일 발송** — `Desktop\북스타\세명대_자료\북스타_퓨처누리_SSO배너_연계값_요청_초안.txt`
   - 배너 등록 + **school_no·portal_user_id 2필드 추가** + HMAC 서명 + 미수령 규격서 2건
2. **도서관(학술정보원)에 별건** — 테스트계정 `book`에 예약 권한 부여 요청
3. **B단계 알림** — 반납임박(D-3/D-1/당일)·예약도착·전자책 만료
   (사이드바 배지가 첫 형태. 앱을 안 열어도 알게 하는 부분이 남음)
4. 배너 오면 **실학생으로 종이책 대출목록·연장·예약 검증**

---

## 7. 8/8 코드 리뷰 후속 수리 (같은 날, 커밋 12개 전수 검토 결과)

1. **공유계정 목록이 학생 것으로 표시되던 구멍** — 개인세션 수립 실패(도서관 사이트 장애·**토큰 7일 만료 후**)
   시 서버가 공유계정으로 폴백하는데, 앱 `smEbookLoans/smEbookReserves`가 `personal` 플래그를 안 봐서
   **공유계정의 대출 목록이 "빌린 책"으로 그려지고 반납 버튼까지 진짜 작동**했다.
   → `d.personal===true` 확인 + 401/409 감지 시 개인 플래그 내리고 "연동 만료 — 배너로 다시 로그인" 안내
2. **작은따옴표 제목이 반납 버튼을 죽임** — esc()의 `&#39;`는 onclick 속성 안에서 `'`로 되돌아와
   JS 문자열이 깨진다(예: Charlotte's Web). → 곡선따옴표(’) 치환
3. **픽업 취소번호 오매칭** — 진행 중 신청이 여럿이면 첫 건을 집어 딴 책을 취소할 수 있었다
   → 제어번호 일치 우선, 없으면 최신 건
4. **saveSession 실패 시 sso_personal=1 거짓 신호** → 저장 성공까지 확인 후 1
5. **공개 테스트 페이지의 비밀번호 기입값 제거** (위 §5 참고)

6. **(8/9 사장님 지시) 미연동자 예약 완전 잠금** — 공유계정(03251) 폴백으로 로그인 없이도
   종이책 예약·찾아줘북즈가 되던 경로를 앱+서버(semyung-reserve 쓰기액션) 양쪽에서 차단.
   미연동자가 예약 누르면 → "도서관 로그인이 필요해요" 시트 → 세명대 로그인
   (setopia `/main/index_lib_smate.jsp`) → 도서관 홈페이지 배너로 복귀하는 흐름 안내.
   ⚠️ 배너가 연계값을 실어주기 전까지는 일반 이용자 예약 불가(의도된 상태).
   전자책 '바로 읽기'의 공유계정 폴백만 체험용으로 유지.

7. **(8/9 사장님 지시) 데모 학생 5명(김민준·이서연·박도윤·최지우·정하준) 완전 삭제** —
   DB 11개 테이블 222행 삭제(백업: `세명대_자료/_데모학생5명_삭제전_백업_20260809.json`).
   "누구로 입장할까요?" 선택창 → 세명대 로그인 안내로 전환, 미로그인 자동 선택창 제거.
   이제 계정이 생기는 길은 [세명대 로그인 → 도서관 배너] 하나. 피드·리더보드는 빈 상태 안내로 시작.
   +후속: **데모 챌린지 3개(7/2 개발용, 학교명 "한국대학교")도 library_programs에서 삭제**
   (백업: `_데모챌린지3개_삭제전_백업_20260809.json`). pruneDeadChals의 "0건이면 무시" 가드를
   "0건도 유효"로 고쳐, 각 브라우저의 '기간이 끝난 챌린지' 잔재가 다음 접속 때 자동 정리되게 함.

남은 관찰 사항(수리 안 함, 판단 대기):
- `borrow`는 개인세션 실패 시 여전히 공유계정으로 조용히 폴백 — 대출은 성공하지만 그 책이
  "내 도서관"에 안 보이는 불일치 가능. 배너 연계값이 정식으로 오면 재검토
- `sso_sessions` 만료 행 청소 없음(누적) — 규모 작아 보류
- sso-login 입구 위조 방어는 퓨처누리 HMAC 배너 서명 대기(기존 계획대로)

## 참고 문서
- `_세명대_도서관_기능_전수목록.md` — 홈페이지 메뉴 76개 분류(버릴 것 포함)
- `_세명대_이관계획_대여예약.md` — 학생 여정 9단계 매핑, 빠진 조각, 순서 A~D
