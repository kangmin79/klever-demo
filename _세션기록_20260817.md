# 세션 기록 2026-08-17 (일) — 관리자 5화면 = 측정 로그 v2 + 외주 요청서

## 배경
- 사장님(김병삼)이 8/16 `Desktop/북스타_관리자_5화면_2026.08.16.html`(대시보드·이용통계·챌린지통계 신설·학생글·운영이력 목업)을 보냄. AI가 그려준 목업으로 "로그 1~6"이 있다는 전제 위에 그렸으나, 우리 DB엔 활동(글·별)만 있고 접속·조회·이용·연결 로그가 없었음.
- 사용자 판단: 이건 요구사항이지 기획이 아님 → 우리가 설계해서 순차 진행. 사장님한테 로그 정의서 묻지 않음(웹·로그 이해 없음).
- 앞선 질문(찾아줘북즈·예약·전자책 대출이 도서관 관리자에 반영되나) → 반영됨(도서관 사이트에 실제 요청 대행). SSO 학생=본인 명의, 미연동=03251 공유계정 명의. 직원 관리자 콘솔은 직접 본 적 없음 → 사장님 확인 후 재논의.

## 완료 (전부 배포, klever_demo main)
| 단계 | 커밋 | 내용 |
|---|---|---|
| 1 로그 설계·테이블 | efdf97d | `_측정로그_설계_20260817.md` + `scripts/sql/bookstar_events.sql` (kind visit/view/read/link/activity, 꼬리표 item_type·origin·program_id·ref·ok·voided·seconds). RLS anon INSERT만. 검증 201/42501/서비스롤 |
| 2 앱 기록 | 7a07e8e (260817a) | index.html `bxEvent` 모듈 + 훅 18곳 + 출처 위임(`.bx-org{display:contents}` 래퍼·정적 data-origin·capture 클릭) + 자동 미인정 4규칙(글자수·연락처/링크·반복·중복)+안내문. 헤드리스 스모크 실적재 PASS |
| 3 집계 | cf30d58 → 보정 0b6d9f0 | `bs_stats_overview/usage/challenges/challenge_detail/curation` + `bs_writing_hide`(글hidden+이벤트voided+별 회수/원복). anon 실행권 없음. admin-save `stats_*`·`writings_hide` op 배포. **보정: 완주=담긴 책 전부×켠 미션 전부(`bs_ch_done_students`), 참가=1건이라도(`bs_ch_part_students`)** — 앱의 완주(⭐30·enroll.done_at)는 책 1권 단위임을 발견 |
| 4 화면 | 1ef2451 | admin 5화면 교체(사장님 시안 CSS) + SheetJS 다중시트 엑셀. 헤드리스 스모크(adminSave 스텁) PASS. 사용자 실로그인 스크린샷으로 집계 호출 정상 확인 |
| 문구 검수 | 0b6d9f0 | 사용자가 화면 문구 5장 검수 → 4개 정확, "완주(앱 판정과 같음)" 틀려서 수정, "(1분 이상)·(신청까지)" 통일, 대시보드 참가/완주 칩 라벨 |
| 테스트 데이터 전삭제 | — | 사용자 지시 "가상 시연 데이터 전부 삭제": events·stars·totals·enroll·reader_stats·minsong_app_events·byeoli_query_logs·byeoli_search_clicks 0행 |
| 코드 청소 | 81d9f6e (260817b) | 관리자 옛 통계/대시보드/학생글/히스토리 278줄 삭제(BX_NAMES·USERS/BOOKS_STAT/MONTHLY·가짜 visits/uses·차트), 학생 앱 김민서 폴백·slot ai 가드 제거, shot_*.mjs→_scratch, gitignore. 5화면+7화면 스모크 PASS. 유지: BX_STUDENTS(7곳 폴백)·app.html(리다이렉트+미리보기)·_bookmark_*(보류 자산)·루트 미추적 bodies_gb*.js 13개(판단 대기) |

## 외주 요청서(RFP)
- `Desktop/북스타_외주개발_요청서_RFP_2026.08.17.txt`(+ `북스타/…md`). 목적: 사장님이 클로드에 넣어 견적을 보게 → 자산 규모 인식(합의서 자산 단독소유 근거).
- 구성: [AI에게 요청] 지시문 → 0장 실측 현황(라이브 URL·1.5만 줄·함수23·테이블41·장서32만·표지23.6만·고전310·번역·퀴즈3,880·연동9종·커밋1,019) → 1~8장. 네이티브 앱 제외(사용자 지시). 콘텐츠=AI 번역 기준.
- 웹 클로드 1차 견적 21억(94.78M/M·공공 제경비 산식)은 과다 → 7장에 가드(민간 시장가·제경비/기술료 금지·코드량 역산·최소안/보통안·마진 15~25%). 목표 구간 **2~4억**.
- 전달법: 파일 통째로 → 견적 본 뒤 "81일에 내가 만들었다".

## 판단·기록
- 완성도 자평 ≈80%. 남은 핵심 4: 보안 잠금 마무리(학생표 11개 SSO 경유) · 뷰어 2순위 · 완주 정의 통일(앱 "1권 완주!"⭐30 vs 사장님 정의) · **학생 파일럿 열기(실사용자 0)**.
- 구조 개편(파일 분리)은 파일럿 후. 청소만 완료.
- 방향 우려: 단일 대학 앱=트래픽 천장(MAU 수백). 권고 = 자산 소유 조항 + 시간 재배분(북스타는 유지 모드, 휙으로).

## 다음 세션 진입
"관리자 5화면 이어서 — klever_demo/_측정로그_설계_20260817.md 보고" 또는 사장님 견적 반응/관리자 콘솔 확인 결과 받은 뒤.
