# 추천 설계 v2 — "울타리 넓이만 다르다" (2026-08-18 확정, 사용자와 합의)

## 원칙
- 도서앱 = 매체. 추천 목적 = **지금 읽을 수 있는 책으로 읽는 습관 만들기**. 성공지표 = 추천→바로읽기 전환(bookstar_events origin)
- 모든 화면이 같은 두 재료: **관련성 = 울타리**(이 안에서만), **인기 = 순서**(울타리 안에서 인기순). 화면마다 울타리 넓이만 다르다
- 많이 보여주지 않는다. 별이 5권.
- 크레마는 이번 설계에서 배제(동시이용 미확인). "바로 읽기" = 대출 가능한 구매 전자책(+자체 본문은 별이 결과에 아직 안 섞임 — 후속)

## 화면별
| 화면 | 울타리 | 순서 | 권수 | 대출중 처리 |
|---|---|---|---|---|
| 전자책 대체 추천 (상세에서 대출중→예약 CTA 아래) | 그 책의 **장르**(class_no 접두: 5자→3자→2자→1자 사다리, 줄거리 X) | 인기(book_pool 국중 대출수) → 신착(reg_date) | 3 | **바로 읽을 수 있는 것만**(재고 실시간) |
| 별이 (질문) | 질문 주제(기존 벡터+리랭킹 rel≥2) | ①바로 읽기 가능 전자책 ②재고 미확인 전자책 ③종이책 소장 → 각 계층 안에서 rel → 인기 → RRF | 5 | 최대 1권, **원래 1위였을 때만** 맨 위에 "대출 중·예약" |
| 사서 큐레이션 | 사서 주제로 좁게 | 관련성 먼저 + 인기 조금(기존 +0.13 캡) | 후보 24 → 게시 8 | 무관(변경 없음) |

## 구현 지도
- `_shared/ebook_stock.ts` — fetchStock(스크래핑) + 3분 캐시 + stockMany(병렬·타임아웃)
- `curate/index.ts` — `body.similar={brcd,title?}` 모드: tulip에서 class_no 조회 → RPC `similar_ebooks` → 시리즈 dedup → 재고 확인 → 3권
- DB RPC `similar_ebooks(class_prefix, exclude_brcd, lim)` — semyung_tulip(ebook) ⨝ book_pool(sm_ebook_brcd) 인기순·신착순
- `byeoli-search/index.ts` — 조립 후 전자책 상위 재고 병렬확인 → 계층 정렬 → caps.books 5, 결과에 `_avail`(true/false/null)
- `index.html` — usCardsHTML/hwcCandCard: `_avail`로 버튼 라벨(바로 읽기 / 대출 중·예약하기 / 종이책 소장) · loadEbookStock 대출중 분기에서 similar 3권 렌더(data-origin="similar")

## 후속(이번에 안 함)
- book_popularity 표(정보나루 45,149 + 세명대 동시대출≥2 136종) — 지금은 book_pool 국중 신호로 시작
- 자체 본문(고전·번역)을 별이 결과에 섞기
- 홈 발견 진열 "바로 읽기만" (크레마 실측 후)
