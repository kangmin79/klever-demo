# 세션 기록 2026-08-29 (저녁) — 학생 웹 전수 리뷰 → 수리 1·2단계

## 한 줄
로그인 연동 전제로 학생 웹 6갈래 전수 리뷰(에이전트 6개) → 계약 전 필수 항목을 두 단계로 수리·배포·실측.

## 배포 커밋 (klever_demo)
- 5978585 **1단계 신원 잠금** — 학생 신원=Supabase Auth 세션(JWT app_metadata.hakbun), 활동 표 9종 RLS 본인만, 학번만 로그인 차단(banner_key), 공개 학생뷰, 비공개 글 노출·이모지 XSS 수리, _sso_test.html 삭제, 로그아웃 정리, 자동 버전 훅 → APP_BUILD 260829a
- 135107e **2단계** — 데모 책 제거·사서 담은 책 우선·가짜 랭킹 제거·뒤로가기·해시 딥링크·iOS16·서버 잠금/상한/타임아웃 → 260829b
- Edge Fn 배포: sso-login, semyung-my, semyung-ebook-borrow, notify-due, semyung-reserve, semyung-holding, semyung-best, evaluate, recommend, chat, curate, byeoli-search. 삭제: semyung-ebook
- DB(Management API SQL): `bx_hakbun()` 함수, `bookstar_students_public` 뷰, 정책 교체(students/writings/results/enroll/reader_stats/mybooks/life_history/shelves/likes/follows/events), guestbook·stars·totals 접근 회수, TRUNCATE/REFERENCES/TRIGGER 전 테이블 회수, writings featured/hidden 트리거 가드, emoji 길이 ≤8 제약

## 설계 결정
- 신원: 서버 함수 경유 방식(호출처 수십 곳 교체) 대신 **Supabase Auth JWT + RLS** — `BX_H.Authorization`을 세션으로 바꾸는 것만으로 기존 호출 70여 곳이 본인 명의가 됨. 옮길 데이터 0(학생 1명·글 0건)이라 정책 교체 안전.
- 로그인 흐름: sso-login → `sso_auth`(magiclink 해시) → 앱 `/auth/v1/verify` → 세션 저장 → 주소 정리 후 재진입(bxAuthRestore가 복원). refresh 자동(만료 2분 전·탭 복귀 시), 다른 탭 갱신 충돌 대비.
- 학번만 로그인: `SSO_BANNER_SECRET` 비어 있으면 3필드 경로 완전 차단. 계약 시 학교 배너에 `banner_key` 실어 보내기로.
- 로그아웃 후 발급된 access 토큰은 최대 1시간 서버에서 유효(Supabase 표준) — 앱은 기기에서 지우므로 실사용 문제 없음.

## 실측 (probe_auth.mjs / probe2 / probe3)
- 학번만 POST 403 · dev 로그인 302(sso_auth 포함, personal=1) · verify 200 · 해시 재사용 403 · refresh 200
- 본인 글 201(featured/hidden 강제 false) · 남의 학번 글 403 · 익명 글쓰기 401 · 익명 비공개 글 0건 · 본인 비공개 글 1건
- 학생표 본인 1행 · 익명 401 · 공개뷰 "독***문" · 이모지 스크립트 400 · 남의 학생행 403 · 남의 점수 403 · 남 명의 팔로우 403 · 익명 남의 학번 이벤트 401 · guest 이벤트 201 · 별 표 401 · 본인 글 삭제 1행
- reserve list → needsLogin · semyung-ebook 404 · recommend/curate 500자 400 · byeoli config 덮어쓰기 무시(정상 결과 5건) · semyung-my pickups 개인세션 ok · evaluate 무토큰 401

## 남은 것
- `_다음세션.md` 참조. 사장님 답 대기 항목(소감 공개 정책 등)은 답 오면 반영.
