# 세션기록 2026-08-29 (북스타 웹)

## 배포된 것 (전부 라이브 확인)
| 커밋 | 내용 |
|---|---|
| 9292107 | 모바일 헤더 두 줄 고정 — 로고·계정칩이 줄어들지 않아 3줄이 되고 헤더 높이는 98px 고정이라 메뉴 탭이 본문 위에 떠 보이던 것. 실제 헤더 높이를 재서 본문 시작에 반영(--header-real) |
| a58c6c8 | 국내(한국) 고전 상세 "퀴즈로 바로 읽기" 숨김 — 퀴즈 3,880문항 전부 세계고전, kr 0개. `b.locale==='modern'`로 판정(화면 탭 아님). 310권 전수 확인 |
| 4dc75a5 | **웹 재고 긁기 0** — 별이검색(10권)·닮은책(24권)·상세배지가 학교 화면을 실시간 긁던 것을 `solsup_stock` 표 읽기로. `_shared/stock_table.ts` 신설. live 경로는 STOCK_LIVE_KEY 시크릿 필요. byeoli-search·curate·semyung-ebook-borrow 3함수 배포. 실측: 검색 37ms, 브라우저가 live 못 켬, stockLive 키 없이 403. 표가 비어 닮은책 0권(정직한 저하) |
| 0a645d8 | 새 큐레이션·챌린지 **맨 위** 삽입(AI·손으로·챌린지 4곳) + `library_programs.sort_order` 열 신설(기존 6개 백필=featured 우선). 챌린지는 순서 저장이 없어 "↕ 순서 바꾸기"가 저장 후 보장 안 되던 것 수리 |
| 7eefe9e | AI 큐레이션: "전부다 (장르 안 가림)" 칩 항상 추가·누르면 직전 주제로 바로 생성 / 권수 버튼 즉시 재선택(aicRepick·aicRenderHint) / 형태 버튼 결과 있으면 즉시 재생성 / 옵션 줄 입력창 위로 / count "+6" 제거 |
| a07dcd1 | **인생책 저장 수리** — bookstar_students 0행·INSERT 경로 없음(8/9 데모 삭제 때 소실)이라 PATCH 0건이 204 성공으로 위장. `bxSetStudent`→`bxEnsureStudentRow`(ignore-duplicates) + `saveLifeBook` 업서트+representation 확인. 프로필 저장도 회복. 서버 E2E 통과 |
| 9743bf2 | 사서 '우수작'(bookstar_writings.featured) 기능 전부 삭제(사장님 지시). 관리자 wrFeature·마이페이지 ★·피드 ★·우선정렬·admin-save featured 경로. admin-save 재배포. '이달의 챌린지'는 별개·유지 |

## 조사로 확정한 것
- 8/28 사장님 PDF 6건 전부 실제 결함. 원인은 전부 다름(에이전트 3갈래 조사, 코드+라이브 DB 실측).
- **학생 글 저장 0건** = anon UPDATE 컬럼 6개 vs 코드 7개(is_public) → 42501. 서버 GRANT 한 줄. (_다음세션.md 1순위)
- 피드 규칙: 공개 체크 글만·최신순·내 글 우선 없음·챌린지 완독 소감은 공개 체크 없이 무조건 피드.
- 팔로잉 탭: 기능 구현됨(팔로우 버튼·저장·필터), 사용 0건.
- 웹이 학교 서버 긁는 곳 전수: 정식 API 9 / 긁기 11→10(재고 제외) / 이미 DB 방식 3(대출랭킹·장서·표지) / 죽은 코드 semyung-reserve. 관리자 화면은 접점 0. 감시 장치 없음. **SSO 붙어도 lib 세션·relation/eBook 2곳은 남음**(퓨처누리 API 필요).
- 관리자 비밀번호 = `minsong`(ADMIN_SECRET, 8/15) — 라이브 ping 200 확인.

## DB 변경
- `library_programs.sort_order integer` 추가 + 6행 백필.
- Edge Function 배포: byeoli-search, curate, semyung-ebook-borrow, admin-save.

## 바탕화면 산출물
- `세명대_긁기0_요청자료\` (01 전수목록 · 02 퓨처누리 요청 · 03 교보 요청 · 04 학교 요청) + `북스타_긁기0_요청정리.txt`

## 교훈
- "이미 표에 넣어둔 데이터를 왜 또 긁나" — 웹(8/18)과 앱(8/19)이 같은 재고 코드를 하루 차이로 만들면서 저장 부분이 앱에만 붙음. 공용 코드는 한쪽만 고치면 다른 쪽이 옛날 방식으로 남는다.
- 저장 성공 판정을 HTTP 코드만 보면 0건 수정도 성공. 업서트+representation으로 실제 값을 확인할 것.
- 같은 이름(featured)이 두 기능(우수작 / 이달의 챌린지)에 쓰임 — 삭제 시 반드시 구분.
