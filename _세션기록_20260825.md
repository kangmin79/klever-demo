# 세션 기록 2026-08-25 (아침~오전)

## 완료
1. **YES24 선택화면 자동 넘기기 성공** (minsong_app App.js `EbookViewerModal`) — 학생 무탭. 진범=뷰어가 팝업인 줄 알고 self-close/opener. 성공조건 4개 주석에. X·검은 띠 제거(폰 뒤로가기로 닫음). 최신 APK: https://expo.dev/artifacts/eas/9DSJPlBIqJ0WzwVUHI8nKExBwQeLdY_YRZe0va59kdo.apk
   ⛔"브라우저로 열기" 비상구 금지(같은 책 두 창=403). ⛔진단은 Alert 말고 하단 글씨줄.
2. **교보(MLSS 김희수) 규격서 PDF + 메타 xlsx 23,813종 수령·분석** — frontapi 7종(로그인 불필요)·에러코드 52. 없는 것=개인 대출·예약 목록 조회(화면으로만, 로그인 상태로 JSON 창구 없음 확정)·재고 API(예정). `contentBorrowList.xml`은 존재하나 무엇을 보내도 False(용도 미상).
3. **메타로 구멍 메움** `scripts/meta_fill_ebook.py`(5f26218): 부수 `ebook_copies`·`ebook_meta_at` 신설 23,019행 / 바코드 연결 193(엄격 매칭만) / 줄거리 289·표지 23 / 사용중지 3(copies=0). 재임베딩 371. "놓친 820"=바코드없는 레코드 226+권별 257+미등록 264+오디오북 65.
4. **재고 sweep 완주 불가 버그 수리** (minsong_server solsup-stock abfd099): 이어받기 창 30→120분. 완주 즉시 잠자던 '가능' 17권 매진 교정.
5. **화면긁기 전수목록** 11곳 → memory `reference_bookstar_scraping_inventory.md`.
6. 죽은 GitHub Actions `semyung-new.yml` 삭제(bbede7a) — 8/7 폐기 스크립트를 부르던 껍데기. 대출랭킹은 로컬 Bookstar_Semyung_Sync 03:50 정상.
7. **교보 문의 메일 문안 확정** → `Desktop\교보 요청 사항.txt` (①대출·예약 목록 ②재고 ③메타 정기). 논리="내부에서 이미 조회하는 데이터를 우리도 조회하게 열어달라". 퓨처누리엔 지금 요청 없음(계약 때 배너 서명만).

## 다음
- 교보 답장 오면 **먼저 보여달라**(용어 풀이·같은 톤 답신). 덧붙일 것: 웹훅(대출·반납 발생 알림)·대출 횟수 통계. 서버부담 카드(하루 ~20만 화면 읽기)는 저쪽이 호출량 언급할 때만.
- sweep 크론 확인: `solsup_cache k=sweep` page가 1→551→1021→1로 도는지(09:07·09:37·10:07 KST).
- 폰 실측: 새 APK(예스24 자동 넘기기·교보 회귀·같은 책 재열기).
- 풀 gated→active 전이(04:00 pool-sync)로 줄거리 채운 289권 진입 확인.
- "1권 남음" 조건 `ebook_copies≥2` 좁히기 — 사용자 결정 대기.
