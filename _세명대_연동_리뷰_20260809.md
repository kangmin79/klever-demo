# 세명대 도서관 연동 — 2026-08-09 작업 리뷰

전날(8/8) 리뷰는 `_세명대_연동_리뷰_20260808.md`. 이 문서는 그 다음 날 두 가지를 다룬다.
커밋 `f963591`(보안), `056fe6c`(알림).

---

## 1. 전자책 공유계정 폴백 폐지 — 익명 대출·남의 책 자동반납 차단

### 무엇이 문제였나
로그인(도서관 계정 연결) 안 한 방문자가 '바로 읽기'를 누르면, 서버가 **관장님 공유계정**으로
대신 빌려주고 있었다. 데모 편의를 위해 남겨 둔 경로였는데 두 가지가 실제로 나빴다.

1. 관장님 실명으로 익명 방문자의 대출 기록이 남는다
2. 5권 한도가 차면 **"가장 오래된 1권 강제 반납"** 로직이 돌아 **남이 읽던 책이 끊긴다**
   (뷰어 토큰이 그 대출건에 묶여 있어 읽던 화면이 죽는다)

8/9 실측 시점에 관장님 계정은 이미 **4/5권**이 차 있었다 — 한 명만 더 누르면 발동할 상태였다.

### 무엇을 했나
- **서버**(`semyung-ebook-borrow`): 재고(`stock`, 공개) 외 **전 액션이 개인세션 필수**.
  없으면 `409 needsPersonal`. borrow의 자동반납 로직은 삭제.
- **`returnAll`**은 원래 **누구나** 부를 수 있어서 관장님 대출을 통째로 반납시킬 수 있었다
  → `SEMYUNG_ADMIN_KEY` 시크릿 대조로 잠금(403). 공유계정 청소용으로만 남김.
- **앱**: '바로 읽기'를 **창을 열기 전에** 막고 로그인 안내 시트로 보낸다.
  `smLoginGuide('read')` 모드 추가(예약 문구 → 대출 문구). 연동 만료(409)도 같은 안내로 회수.

### 확인
익명 `status`/`myLoans`/`borrow` 전부 409, `returnAll` 403, `stock`은 200 정상.

### 남은 것
관장님 계정에 **8/9 이전에 빌려진 전자책 4권이 그대로 있다**(438470·438674·438675·438676).
새로 쌓이지는 않으니 두면 14일 뒤 만료된다. 지금 비우려면:

    GET .../semyung-ebook-borrow?action=returnAll&key=<SEMYUNG_ADMIN_KEY>

⚠️ 누군가 읽고 있는 중이면 그 화면이 끊긴다 — 방금 없앤 바로 그 피해다. 급하지 않으면 두는 쪽.

---

## 2. B단계 알림 — 앱을 안 열어도 알게 하기

지금까지 반납일은 **앱을 열어야만** 알 수 있었다(사이드바 배지 = A단계). 그 반대편을 만들었다.

### 구조

| 조각 | 하는 일 |
|---|---|
| `_shared/alerts.ts` | **판단 엔진(순수함수)**. 무엇을 언제 보낼지. 단위테스트 25개 |
| `notify-due` | 하루 1회 배치. 학생당 도서관 1회 조회 → 판단 → 발송 |
| `push-register` | 구독 등록·해제·즉시 테스트 |
| `_shared/push.ts` | 실제 발송(VAPID 서명 + 본문 암호화) |
| `sw.js` | 브라우저가 알림을 받아 띄우는 서비스워커 |
| `manifest.json` + `icon-192/512.png` | 홈 화면에 추가(아이폰 푸시의 전제조건) |
| `push_subs` 표 | 구독 보관. RLS 잠금, service_role 전용 |

### 알림 규칙 (엔진에 한 곳으로 모음)
- 우선순위: **연체 > 예약도착 > 반납 당일 > 내일 > 사흘 전**
- **하루 한 통만.** 나머지는 "외 n권"으로 접는다 — 여러 통 오면 사람은 알림을 꺼버리고,
  끄면 그 뒤로는 아무것도 전할 수 없다
- **D-2·D-4는 일부러 안 보낸다**(매일 울리면 무시하게 된다)
- 연체·예약도착은 해결될 때까지 매일(연체료·예약정지가 걸린 문제라)
- 날짜는 **KST 기준** — 엣지는 UTC라 그냥 쓰면 하루가 밀린다

### 발송 시각
`pg_cron` + `pg_net`, 매일 **00:00 UTC = 09:00 KST** (잡 이름 `bookstar-notify-due`).
사장님 PC 스케줄러가 아니라 Supabase에서 돈다 — PC가 꺼져 있어도 나간다.

### 결정적으로 조심한 것
- **서비스워커에 `fetch` 핸들러를 넣지 않았다.** app.html은 CSS·JS가 전부 인라인된 단일 파일이라
  서비스워커가 응답을 캐시하기 시작하면 **배포해도 옛 앱이 계속 뜬다**(되돌리기도 어렵다).
  fetch를 안 잡으면 브라우저는 늘 서버에서 받아온다 → 캐시 위험 0.
- **아이폰은 홈 화면에 추가해야만 웹푸시가 온다**(iOS 16.4+ 제약). 그냥 '켜기'를 보여주면
  눌러도 안 오는 상태가 된다 → 앱이 iOS·미설치를 감지해 **설치 안내를 대신 띄운다**.
- **7일 세션 만료를 그대로 두면 기능이 무의미**하다("일주일 안 들어오면 알림 끊김").
  알림을 켠 학생은 조회가 성공할 때마다 만료를 30일씩 민다. 연계값이 죽으면 조회가 실패하고
  5회 연속 실패 시 구독이 꺼진다 — 죽은 세션이 영원히 남지 않는다.
- 브라우저 구독은 됐는데 **서버 등록이 실패하면** "켜진 것처럼 보이는데 안 오는" 상태가 된다
  → 그 경우 브라우저 구독을 되돌린다.

### 확인한 것
- 판단 엔진 **단위테스트 25/25** — 경계일(D-0/1/2/3/4), 우선순위, KST 하루밀림, 중복키, 제목 축약
- **실계정 배치**: 테스트계정(`book`)으로 전자책 1권 대출 → 배치가 `loans:1`로 인식,
  반납일이 14일 뒤라 알림 없음(정상). 반납까지 하고 잔여 0건 확인
- **발송 경로**: FCM이 VAPID 서명과 aes128gcm 본문을 **받아들였고**, 가짜 endpoint라 410 응답
  → 죽은 구독 자동 삭제까지 동작. (암호화가 틀렸으면 400/401이 왔을 것)
- `pg_net` → `notify-due` 200. 테스트 세션·구독 전량 삭제 완료

### ❗아직 못 본 것 — 사람이 눌러야 하는 부분
브라우저 구독 생성은 **사용자 제스처**가 필요해서 자동 검증이 불가능하다. 1회 확인 부탁:

1. `bookstar.co.kr/_sso_test.html` → 아래 초록 박스 〔개인기능까지 열고 로그인〕
   (아이디 `book`, 비밀번호 `semyung7002` 직접 입력)
2. 사이드바 **내 도서관** → 맨 아래 **"반납일 알림"** → **켜기** → 브라우저 권한 허용
   → 곧바로 확인용 알림 1건이 와야 한다
3. 안 오면 아무 날에나 강제로 한 번 더 보낼 수 있다:
   `GET .../notify-due?key=<SEMYUNG_ADMIN_KEY>&force=1`
4. 확인 끝나면 **끄기**를 눌러 구독 삭제

⚠️ 아이폰 사파리 탭에서는 안 온다(설계대로). 홈 화면에 추가한 뒤 그 아이콘으로 열어야 한다.

---

## 3. 전자책 전수 재점검 (8/9 추가, 커밋 `ac8f20b`)

"전자책 쪽 안 되는 게 없나" 확인 요청으로 **실제 도서관에 대고 전 기능을 눌러 봤다.**

### 전부 동작 (실물 검증, 원상복구 완료)
| 기능 | 결과 |
|---|---|
| 재고(공개) | 대출 2/5 · 예약 0 · 버튼 판정 정확 |
| 이용 상태 | 본인세션 · 한도 5 · 현재 0권 |
| 대출 → 뷰어URL | loanSrmb 발급, 뷰어 URL 실제 응답 302 |
| 내 대출목록 | 제목·반납예정일·**바코드 정확 추출**·연장가능 판정 |
| 연장 | 8/14 → **8/19** (5일 연장) |
| 재고 반영 | 2/5 → 3/5 → 반납 후 2/5 원복 |
| 반납 | brcd 없이 loanSrmb만으로 성립 |
| **예약 → 취소** | 예약 0명 → **1명** → 취소 → 0명, 예약번호(prenSrmb) 정상 |

앱 배선도 전 액션에 호출부가 있다(`status`만 미사용 — 앱은 목록으로 직접 세므로 불필요).

### 🐛 잡은 것 — 대출기간을 우리가 지어내고 있었다
`"대출 완료 — 대출기간 14일"`이 하드코딩돼 있었는데 **실제는 5일**이다.
→ 숫자를 적지 말고 **도서관이 준 반납예정일**을 그대로 쓰도록 고쳤다.
→ 내 서재의 빌린 전자책에도 반납예정일 한 줄 추가(임박 주황, 당일·연체 빨강).
5일은 짧아서 안 알려주면 읽던 책이 어느 날 갑자기 사라진다.

### 🔑 전자책 예약은 "도착 알림"이 필요 없다
교보 예약목록 페이지 원문: **"예약하신 콘텐츠는 대출이 가능시 자동대출되며"**
→ 종이책처럼 "도착했으니 찾아가세요" 상태가 없다. 자리가 나면 알아서 대출로 바뀐다.
→ 알림 엔진이 예약도착을 **종이책만** 다루는 것은 빠뜨린 게 아니라 맞다.
   자동대출된 뒤엔 반납임박 알림이 이어받는다.

### ⚠️ 확인 못 한 것
- **찾아줘북즈 시트의 "대출기간 14일"(종이책)** — 도서관 안내 페이지가 본문을 서버에서
  안 내려줘서(`/webcontent/info/28`이 메뉴만 반환) 근거를 못 봤다. **틀렸다는 증거도 없어
  그대로 뒀다.** 도서관에 물어보거나 실제 종이책 대출 1건이 생기면 바로 확정된다.
  (전자책 5일과는 다른 시스템이라 같이 묶어 판단하면 안 된다)
- 전자책 뷰어링크 없는 51건은 도서관 원본 결손 — 앱은 통합검색으로 폴백(깨진 버튼 없음)

---

## 4. 다음 할 일 (8/8 목록에서 갱신)

1. **⭐메일 발송** — `Desktop\북스타\세명대_자료\북스타_퓨처누리_SSO배너_연계값_요청_초안.txt`
   배너 등록 + **school_no·portal_user_id 2필드** + HMAC 서명 + 미수령 규격서 2건.
   **여전히 이것 하나가 전체 병목이다** — 코드는 배너만 오면 즉시 작동한다.
2. **도서관(학술정보원)에 별건** — 테스트계정 `book`에 예약 권한 부여 요청
3. **알림 실기기 확인**(위 §2 4단계)
4. 배너 오면 **실학생으로 종이책 대출목록·연장·예약 검증**

## 참고
- 시크릿: `SEMYUNG_ADMIN_KEY`(returnAll·notify-due 관리자 키), `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`
- 공개키는 앱에도 그대로 들어 있다(원래 공개용). 비밀키만 서버 시크릿.
