# 다음 세션: 계약 전 보안·검수 점검 (2026-08-15 실측 기반)

시작 프롬프트: "계약 전 점검 이어서 — klever_demo/_다음세션_계약전점검.md 보고"

## ⛔ 건드리지 말 것 (사용자 지시)
- bookstar.co.kr/admin (랜딩 저장소의 옛 관리자 복사본) — **그대로 둔다**

## 1. ⓐ anon 쓰기 잠금 — 지금 방식(admin-save 확장)으로 가능한 것부터
2026-08-15 pg_policy 실측 (Mgmt API, 토큰=hwik-web/.env SUPABASE_ACCESS_TOKEN):

**관리자 콘텐츠 (admin/index.html이 anon으로 직접 씀 → admin-save op 추가 후 정책 drop):**
- `library_notices` (DELETE,INSERT,UPDATE 열림)
- `minsong_popups` (DELETE,INSERT,UPDATE)
- `community_posts` (DELETE,INSERT,UPDATE)
- `library_books` (INSERT,UPDATE)
- 참고: 큐레이션 2개(library_sections/programs)는 8/15 잠금 완료 — admin-save Edge Function(op 5종) 패턴 그대로 확장하면 됨. ADMIN_SECRET=minsong

**학생 정당 쓰기가 섞인 것 (부분 잠금):**
- `reviews`: INSERT는 남기고 **UPDATE/DELETE 정책만 drop** (남의 서평 수정·삭제 차단). admin의 서평 삭제(2952)는 admin-save op로 이전
- `bookstar_writings`: ALL 열림 — admin PATCH(2325)는 admin-save로, 학생 INSERT는 유지. UPDATE/DELETE 정책 축소 검토

**운영/정적 데이터 (클라 쓰기 없어야 정상 — 쓰는 곳 grep 후 write 정책 drop):**
- `bookstar_quiz_items` (ALL — 구 audit의 quiz_items 후신), `bookstar_schools` (ALL)
- RLS OFF 3개: `ai_curation_usage`, `mirror_pushed`, `semyung_backfill_health` → RLS 켜고 anon SELECT만(서버는 service_role이라 무영향. 단 **로컬 스케줄러/스크립트가 anon으로 쓰는지 먼저 grep** — tulip_daily 등)

절차: 각 테이블 쓰기 호출처 grep(웹 index.html·admin·minsong_app) → admin-save op 추가 → 페이지 교체 → 배포 → 정책 drop → probe 검증 (큐레이션 잠금과 동일 순서, 다운타임 0)

## 2. ⓑ 학생 테이블 14개 — 계약 전엔 로드맵 문서화만
`bookstar_students/stars/student_totals/reader_stats/shelves/challenge_enroll/challenge_results/follows/guestbook/likes/life_history` 등 ALL 열림.
- 제대로 잠그려면 학생 본인 인증(sso_sessions 토큰) 기반 Edge Function 경유로 전환 — 큰 작업, 별도 세션
- 계약 시점엔 "파악 완료+로드맵" 1페이지로 정리 (리스크 숨기지 말 것)

## 3. 검수 grep
- CHARM 문구: "졸업요건/필수/강제" 표현 금지 (2회 재발 이력) — 웹+앱 전체 grep
- 데모 잔재: 결산(8/15)·관리자(8/14)는 제거 완료. 나머지 화면 훑기 (Math.random·하드코딩 수치·'데모' 문자열)

## 참고
- 뷰어 리뷰 2·3순위 잔여는 별도 (메모리 project_bookstar_viewer_review_20260815)
- KIPRIS '북스타' 상표 수동검색 + 합의서 서명 준비 = 사용자/사장님 몫
