# 다음 세션 — 추천 설계 붙이기

시작 문장: **"추천 설계 이어서"**
작업 폴더: `cd Desktop\북스타\klever_demo`
근거 문서: `_세션기록_20260818.md` 의 "8/18 오전 세션" 절

---

## 지금 상태 (8/18 완료)
- **줄거리 재임베딩 완주** — 206,638권이 줄거리 실린 벡터를 가진다. HNSW 재빌드·Micro 복귀·vacuum까지 끝
- 추천 규칙은 대화로 확정됨(아래). **코드에는 아직 하나도 안 붙었다**

## 확정된 규칙 (그대로 구현하면 됨)
1. 관련성으로 후보 → 2. **도장(`''`) 찍힌 책만 제외**(미조회 `null`·신착은 통과)
→ 3. **시리즈·판본 중복만 대표 1권**(저자는 상한만) → 4. 인지도 정렬 → 5. **0건이면 조건 풀고 재검색**
- **특정 도서 지목 검색은 어디서도 필터 없음**
- 화면별 다이얼: 대체재=인지도 최대+대출가능 필터 / 별이=질문이 막연할수록 인지도↑, 대출가능은 표시만
  / 발견·홈=전국 인기+큐레이션(학교 과거 대출 데이터 쓰지 말 것 — 편향 재생산)

## 할 일 (순서)
1. **인기 신호 표 만들기** — 정보나루 `loanItemSrch`로 45,149종 수집(기간·`age=20`·`kdc=0~9`, pageSize 200, 300여 호출)
   → **별도 좁은 표 `book_popularity`**(isbn13, score, src, updated_at). ⛔semyung_tulip에 컬럼 달지 말 것
2. **대체재 추천** — 전자책 상세 '예약' 아래 "지금 바로 읽을 수 있는 비슷한 책" 3~5권.
   측정은 `bookstar_events origin='similar'`. 반나절
3. **별이·큐레이션에 같은 엔진 적용** — 다이얼만 다르게
4. **`semyung_loan_rank` 수리** — 76행뿐·8/4 이후 멈춤. 수집 작업 `Bookstar_Semyung_Sync`(03:50) 점검
5. 신착 결손 감시(최근 30일 표지·줄거리 빠진 수 매일 집계)

## 잊지 말 것
- 대학도서관 대출의 **77%가 전국 인기목록 밖**(전공서). 인기를 **후보 풀로 쓰면 안 되고 정렬 신호로만**
- 그 77%조차 "검색창뿐인 홈페이지"가 만든 결과 — 발견 화면은 인기·큐레이션으로 새 수요를 만드는 자리
- 외서 일괄 배제 금지: 독일어 대출률 1.28%(한국어 0.80%보다 높다). 중국어만 0.04%
- 신착의 줄거리가 나중에 채워지면 **재임베딩 필요**(`tulip_sync.py --reembed`, 대상 소량)
