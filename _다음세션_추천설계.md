# 다음 세션 — 추천 v2 후속

시작 문장: **"추천 v2 이어서"**
작업 폴더: `cd Desktop\북스타\klever_demo`
근거 문서: `_추천설계_20260818.md`(확정 설계) · `_세션기록_20260818.md` 8/18 오후 절

---

## 8/18 오후 완료 (라이브: e1b7808 · b5c4114 · adfc7c8 = APP_BUILD 260818c)
- 원칙: **"울타리(관련성) 넓이만 다르다, 인기=순서"** · 도서앱=매체 · 즉시읽기 우선
- byeoli-search: 별이 **5권**, 전자책 상위 10권 재고 실시간 → ①바로읽기(고전 포함) ②미확인 ③종이, 대출중은 원래 1위일 때만 1권, 시리즈 dedup, 종이·전자 같은 책 병합, `_avail`
- curate `similar` 모드: 대출 중 전자책 상세 → "같은 장르(KDC 사다리)·인기·재고 있는" 3권(`.ebsim`, origin=similar)
- **고전 310권 임베딩** `classic_embeddings` + `match_classics` → 별이 4번째 소스(항상 바로 읽기). 재생성=`scripts/embed_classics.mjs`
- 공용 `_shared/ebook_stock.ts`(스크래핑+3분 캐시), DB `similar_ebooks` RPC, `idx_tulip_kind_class`

## ✅ 8/18 저녁 추가 (93f2f51 = APP_BUILD 260818d)
- Anthropic 크레딧 충전 + 새 키 배포 → 검수·답변 복구 확인(한강 소설 5권 전부 전자책 바로읽기, 오스틴『설득』 탈락)
- OpenAI·Gemini 키 교체. Gemini는 hwik-web/.env로 이관(표지 스크립트 16개 재지정). 정리=메모리 reference_api_keys_locations_billing
- 대체추천 카드 클릭 → 상세(표지·설명·재고)로 이동 후 '전자책 바로 읽기'로 대출(사용자 지시). 칩 "지금 대출 가능". 상세에 data-origin=similar 유지
- 전자도서관 예약 규칙 확인(이용안내): 반납되면 **자동 대출**, 단 한도 5권 다 썼으면 '예약자우선기간'에 내서재에서 직접 대출

## 🔜 미결(사용자 답 대기)
- 예약 안내 문구 "반납되면 알려드려요" — 알림 기능 없음. ① 문구를 "반납되면 자동으로 대출돼요 · 내서재에서 확인"으로 ② 예약도착 알림 구현(B단계) 중 택일

## 🚨 먼저 확인
1. **Anthropic API 크레딧** — 8/18 저녁 충전·복구됨. Auto-reload 권장 — 8/18 14시경 "credit balance is too low". 충전 전엔 curate 분류·검수·별이 답변이 전부 죽어 품질 판단 불가(예: "설득 심리학"에 오스틴『설득』이 낌). 충전 후 `scratchpad`의 smoke_reco.mjs 같은 3질의로 rel 값 확인
2. 라이브 헤드리스 스모크: `ui_smoke_reco.mjs`(별이 카드 라벨·대체추천 3권·고전 카드 openDetail)

## 할 일 (순서)
1. **크레마 동시이용 실측** — 홈 "바로 읽기만" 진열의 갈림길(사용자 지시로 이번엔 배제)
2. `book_popularity` 좁은 표: 정보나루 45,149종(pop_overlap.py 방식) + 세명대 동시대출≥2(136종, `전자책_대출실측/paper_stock_20260814.jsonl`) + 신착 → similar/별이 인기 신호를 book_pool(9,887)에서 확장
3. 별이 결과의 학위논문(find thesis) 노이즈 — 즉시읽기 원칙상 뒤로/제외 검토
4. 사서 큐레이션 게시 8권 권장(코드 변경 없음, 사서 안내)
5. 별이 오프토픽 게이트 오탐('사랑과 결혼에 대한 영국 소설' 거절) — keyword/find 외에 **classics n≥1도 거절 뒤집기 조건**에 추가
