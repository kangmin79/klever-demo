# 북스타 학생 웹 전수 리뷰 — 2026-08-29 (수정 없음, 파악만)

전제: SSO 로그인이 연동돼 학생이 학번으로 들어와 쓴다.
범위: index.html(학생 앱 전체) · supabase/functions 22개 · sw.js/manifest · 보조 페이지.
점검 방식: 로그인/신원 · 홈/검색/상세 · 읽기 뷰어/퀴즈 · 피드/챌린지/내서재 · 도서관 연동 서버 · 앱 껍데기 6갈래.

---

## 한 줄 결론
**개인 도서관 기능(대출·예약·전자책)은 서명 토큰으로 잘 잠겨 있다.** 문제는 그 토큰을 **발급하는 입구**와, 앱 안의 **활동 기록(글·챌린지·팔로우·점수)** 두 군데다. 여기는 브라우저가 말하는 학번을 서버가 그대로 믿는다. 나머지는 데모 찌꺼기, 학교 서버 접속량, 버전 관리, 자잘한 기능 버그다.

---

## A. 신원·보안 (계약 전 반드시)

### A1. 학번만 알면 남으로 로그인 — 남의 대출·예약·전자책까지 [높음]
- `sso-login/index.ts:153-181` POST에 `school=semyung.ac.kr` + `client_userid=학번` 두 값만 보내면 비밀키 검사 없이 그 학번의 서명 토큰이 나온다.
- 더 큰 문제: 그 학번이 한 번이라도 포털로 로그인한 적 있으면(`:169-177`, `_shared/sso_store.ts:50-57`) 저장된 연계값을 꺼내 **도서관 개인 세션까지 그대로** 만들어 준다 → 남의 빌린 책 조회, 예약·연장, 전자책 대출·반납 전부 가능. 만료도 안 본다.
- 앱 쪽도 같음: `index.html:11867-11871` 주소창에 `?sso_uid=학번&sso_name=이름`만 붙이면 그 학번으로 "로그인"된다(`sso_token`은 저장만 하고 대조 안 함).
- 공개 경로에 `_sso_test.html`이 남아 있어 학번·이름 손으로 넣고 정식 토큰 발급 가능(`:37-48`).
- GET 개발 경로(`sso-login:123-137`)는 `SSO_DEV_KEY` 하나 새면 전교생 순회 가능.
- 연계값(school_no·portal_user_id)을 요청 본문에서 그대로 받아 저장(`:163-164`) — 사실상 비밀번호인데 검증 없음.

### A2. 활동 기록 표 8종이 anon 키로 전체 읽기·쓰기·삭제 개방 [높음]
- `index.html:6303, 6314, 6347, 9227, 9811, 9904, 10012, 10199, 11636, 11649, 5363` — challenge_results / reader_stats / writings / mybooks / students / likes / follows / challenge_enroll 를 브라우저가 고른 `student_id`로 직접 upsert/DELETE.
- 결과: 콘솔 한 줄로 남의 완독 점수 100점, 남의 글 덮어쓰기(같은 book_id·activity로 upsert), 남의 인생책·상태메시지 변경, 남의 팔로우 삭제, 서평 좋아요 숫자 `likes:999999` PATCH(`10908`), 측정로그에 남의 학번으로 수천 건 넣어 관리자 통계 오염(`6233`).
- 좋아요·팔로우·챌린지 참여는 로그인 게이트 자체가 없어 비로그인이면 `'guest'` 학번으로 저장됨(`11628, 11641, 5361`).
- (A1과 뿌리가 같음. 로드맵 ⓑ "활동 쓰기 전부 토큰 검증 함수 경유 → anon 정책 drop"으로 한 번에 닫힘)

### A3. 전교생 학번+실명이 anon으로 통째로 내려옴 [높음]
- `index.html:11614` `bookstar_students?select=id,name,emoji` 전체 조회. 화면은 `pubName()`으로 가리지만 네트워크 탭엔 원본. `11720`은 인생책 이유까지.
- limit 없어 1000명 넘으면 잘림(이름이 '학생'으로).

### A4. 다른 학생의 "나만 보기" 글이 프로필 모달에서 노출 [높음]
- `index.html:11800` `openStudentProfile`이 `is_public=eq.true` 없이 가져옴. A가 비공개 서평 저장 → B가 피드에서 A 이름 클릭 → 본문 보임. "남긴 글" 개수도 비공개 포함(`11832`).

### A5. 이모지 칸으로 저장형 XSS [높음]
- `bookstar_students.emoji`는 anon PATCH로 아무 문자열이나 저장 가능(`10199`) → `11688, 11790, 11825`에서 이스케이프 없이 innerHTML. 피드를 여는 모든 학생 브라우저에서 실행.

### A6. 인증 없는 서버 함수 [높음~중간]
- `semyung-ebook/index.ts:21-27` 아무 user_id로 YES24 열람 토큰 발급(주석에 "데모, 클라 신뢰 금지"라 적혀 있지만 배포돼 있으면 열림). http 평문(`:16`).
- `semyung-reserve/index.ts:105-120` list/holdlist 무인증 → 관장님 공유계정(03251) 신청현황 외부 노출 + 호출마다 학교 서버 로그인 2회.
- AI 함수 전부 무인증·무제한: `evaluate:93-100`(Sonnet 12,000자), `recommend:121`, `chat:139-147`(책 2,000권 프롬프트 매번), `curate:450`(월 한도는 sonnet일 때만 `:602`), `byeoli-search:371-375`(클라이언트 config로 서버 설정 덮어씀). 스크립트로 반복 호출 시 하루 수십만 원.
- `tulip-cover:40-63` 무인증 DB 쓰기·알라딘 한도 소진. 알라딘 키 소스 박힘(`tulip-cover:14`, `bookinfo:5`). 정보나루 키 http 평문(`find-in-library:83`).
- `push-register:49-53` unsubscribe 주인 확인 없음, endpoint URL 검증 없음.
- `admin-save:19-20` 비밀번호 사전 단어·시도 제한 없음·CORS `*`.

### A7. 토큰이 URL에 실려 옴 — 공용 PC 7일 [중간]
- `sso-login/index.ts:104-111` 302 Location 쿼리에 `sso_token`·학번·이름. `index.html:11880` replaceState는 이미 기록·CDN 로그·확장프로그램에 남은 뒤. 유효 7일(`sso_token.ts:35`), 로그아웃해도 서버 세션 무효화 없음(`sso_store.ts:26-34`). 열람실 PC에서 다음 사람이 방문기록으로 열면 그대로 대출 화면.

### A8. 로그인 상태 관리 구멍 [중간]
- 게이트가 두 기준(`bxStudent()` vs `ssoIsPersonal()`)으로 갈라짐. `ssoIsPersonal`은 localStorage `'1'` 하나(`7075`).
- 토큰 만료(7일) 후 401을 처리하는 코드 0건 → "빌린 책 없음"으로 조용히 표시, 반납일 배지 사라짐(`7081-7084, 7369, 7378`).
- 로그아웃이 키 3개만 지움(`6468-6477`). 임시저장 글·챌린지 답안·참여목록은 남음.
- 게스트로 읽던 진행이 다음에 로그인한 아무 학번으로 승계(`11878 _migrateChalRecords`) → 공용 PC에서 남의 완독율이 내 기록으로 서버 저장.
- `smPortalLogin`이 이름을 빈값으로 보내 이름 자리에 학번 저장(`5554` → `sso-login:159` → `6165`) → 헤더·피드에 학번 노출.

---

## B. 데모 찌꺼기 · 학교 서버 접속량

### B1. '프로젝트 헤일메리' 데모 책이 실제 학생 화면에 노출 [높음]
- `index.html:5799-5801` 데모 계정 `00003251` 박힌 `http://ebook.semyung.ac.kr:8082/...` 링크. `6582`에서 풀 맨 앞, `6126` 홈 첫 줄, `11192` 검색에 섞임. `7060/7490`은 `b.lib` 있으면 외부 뷰어 바로 열기 → 학생이 누르면 **남의(데모) 계정 명의** YES24 뷰어가 http로 열리거나, 바코드 없어 대출 실패.

### B2. 홈 '사서가 주목한 책' 칸이 사서가 담은 책을 무시 [높음]
- `6125-6128` `slot==='row'`면 하드코딩 `HAILMARY`+`SEMYUNG_BEST` 11권 고정. 관리자에서 바꿔도 학생 화면 그대로. 부제 비면 데모 문구.

### B3. 대출 랭킹 데이터 없으면 가짜 순위·가짜 ▲배지 [중간]
- `6043-6057` `SEMYUNG_LOANRANK` null이면 풀을 `loan`으로 정렬, `chgBadge` 고정 배열 `['▲ 3','▲ 9','NEW',…]` 순서대로. 카테고리 칩은 색만 바뀌고 거르지 않음(`6040`).
- `6096` 서버가 다 죽어도 데모 책으로 채워져 오류를 알 수 없음.

### B4. 학교 서버 접속 — 정지 방침과 어긋나는 곳 [중간]
- `5901-5907` `loadSemyungBest`가 홈·검색 진입마다 `semyung-best?kind=best/new` 실시간 호출, 실패하면 다음 진입마다 재호출(`5070, 5091`), 빨리 두 번 누르면 병렬 2회.
- `semyung-ebook-borrow:273-282` + `_shared/semyung_session.ts:85-146` 전자책 클릭 한 번마다 로그인 체인 5~12회(세션 캐시 없음). "대출→뷰어→목록" 3번이면 20회 가까이.
- `semyung-my:50-55` 소장조회 캐시 없음(`semyung-holding`은 90초 캐시).
- 학교 서버로 가는 fetch에 타임아웃 0건(`semyung_session.ts:43,54`, `tulip_api.ts:35`, reserve/holding/best) → 느려지면 학생 화면 무한 회전.

---

## C. 기능 버그

### C1. 새 버전 안내가 8/22 이후 안 뜸 [높음]
- `index.html:8120` `APP_BUILD='260822b'`, `version.txt`도 `260822b`. 그 후 8번 이상 배포(글 저장 수리·별 폐지·안내카드). 열어둔 학생은 옛 화면에서 이미 고친 버그를 계속 겪음. `BODIES_VER`(`8123`)도 8/27 본문 수정 후 안 올림.

### C2. 챌린지 완독 소감이 체크 없이 무조건 공개 + 기존 비공개 글 뒤집힘 [높음] — 사장님 질문 답
- `9229` `mpSubmitWrite`가 `is_public:true` 강제, 같은 키로 upsert라 앞서 "나만 보기"로 저장한 글이 공개로 바뀜. 안내 문구 없음(`8899`).
- `challenge_results.impression`(완독 인증·채팅·장면 소감)은 공개 옵션도 사서 숨김 경로도 없음(`11712-11716`, admin은 writings만 숨김).

### C3. 글 수정 경로 데이터 꼬임 [중간]
- `9901-9905` 내서재에서 수정하면 `challenge_id`가 null로 덮임 → 관리자 챌린지 통계에서 빠짐.
- `9927 → 9858` `impression` 글 "수정"이 `oneline`으로 열려 빈 에디터·저장하면 소감 2개.
- `9864-9865, 9885` 기존 글 있으면 임시저장 안 됨 → 30분 고치다 닫히면 유실.
- 학생이 자기 글을 지우는 경로 없음(hidden PATCH·DELETE 0건).
- `8913-8934` 미션 패널이 낸 글을 잠그되 본문은 안 보여줌(재제출 불가).

### C4. 퀴즈 [중간]
- 정답이 클라이언트에 내려오고(`8952` select에 correct) 채점·점수도 브라우저(`9199-9204, 9258-9263`). 완주 판정(`9276-9280`)이 이 숫자.
- `8917/9240` 유형 다른 퀴즈끼리 진행률 섞임 → 작품이해 10문항 풀면 인문성찰 열자마자 10/10 완주.
- `8964-8966` 문항 `opts` null이면 퀴즈 박스 빈 채 멈춤.
- solo(퀴즈로 바로 읽기)는 저장 안 함 → 새로고침 시 소실.

### C5. 본문 우선순위가 메모리 기록과 반대 [중간]
- `8448-8452` gb- 책은 서버 `body_sent`를 먼저 보고, 있으면 로컬 `bodies_gb*.js`를 안 읽음. 8/27 로컬로 고친 목차 35권이 서버 body_sent 있는 책에선 미반영 가능. 진본을 한 곳으로 정해야 함.
- 목차 정규식(`8516`) "Part I had…", "Act like…" 오탐. 앞머리 목차 덩어리가 빈 장 수십 개로(`bodyToHtml 8529`·`sentHtml 8720`엔 필터 없음). 한 장짜리 책은 "CHAPTER I"가 본문 글자로(`8529`). 번역 칸 목차가 영어(`bodies_gb2834.js`).
- `8631` 서버 조회 실패(429/5xx)가 "본문 없음"과 똑같이 보임.
- 읽던 위치 슬롯이 책당 1개(`3732`), 모드 전환 직후 0으로 덮어씀(`8499, 3141` 타이머 미해제). 모바일 원문/번역 탭 전환 시 1장 1쪽으로(`3669`).

### C6. 뒤로가기 3건 [중간]
- 로그인 직후 책 열고 안드로이드 뒤로가기 → 앱 이탈(`8400` openViewer가 히스토리 안 남김, `5029-5035`).
- 별이 채팅 전체화면에서 뒤로가기 → 채팅 안 닫힘(`5039`가 `display==='flex'` 검사하지만 실제는 `.on` 클래스 `12173`).
- 푸시 알림 눌러도 첫 화면(`sw.js:24,30` → `/#mylib`인데 `11946-11947`은 `?page=`만 읽음).

### C7. 검색·상세 [중간]
- `11232-11238, 11261` 같은 ISBN 종이책·전자책이 서로 덮어씀 → 전자책 카드 눌러도 종이책 상세, '바로 읽기' 줄 사라짐.
- `5433-5456` A책 열고 바로 B책 열면 A의 "이미 신청한 책" 표시·취소번호가 B 창에 붙음.
- `11132-11147, 11391-11409` 별이 답변 대기 중 키워드 탭으로 바꾸면 채팅이 검색 결과를 덮음.
- `11215-11231` 장서 검색 서버 오류가 "결과 없어요"로 표시.
- `5138-5140` 챌린지 탭 진입 시 "없어요"가 먼저 번쩍, 조회 실패 시 그대로 남음(`5151` catch 삼킴).

### C8. 피드·내서재 [중간~낮음]
- `11706-11720` 하드캡 200/120/200 + 페이지네이션 없음. 팔로잉 탭은 그 200건 안에서만 → 친구 글 201번째면 "없어요".
- `11716` 소감 카드 시각이 `updated_at`이라 퀴즈 한 문제마다 피드 최상단 재부상, 좋아요 매번 0 리셋(`11673 likeKey`).
- `7124` push-register 응답 JSON 아니면 예외 → 브라우저는 "켜짐", 서버 미등록.
- `10122` 완료한 챌린지가 고전 3권류만 집계, 프로필 모달(`11809`)과 숫자 다름. 완독 기준 95%/90% 화면마다 다름(`9609, 10993, 10700`).
- 점수 문구 잔재 "완독 인증 +50점", "+10점", "장당 +30" (`4642, 4660, 10306, 9375, 9477, 10271, 3069, 8787`) — 별 폐지와 별개 체계면 유지 의도인지 확인.
- 미션 글자수 미달 시 `alert()`(`9223, 9230`) — 8/21 지시와 어긋남.

### C9. 호환·껍데기 [중간~낮음]
- `5941` 정규식 lookbehind 1곳 → iOS 16.3 이하 사파리에서 본체 스크립트 전체가 안 돌아 흰 화면.
- `11939-11944` 학생이 못 가는 International·커뮤니티 화면을 매 접속마다 렌더·서버 조회(관리자 International 영역은 죽은 칸).
- 전역 오류 핸들러 0건, 빈 catch 185곳, 초기화 try 없음(`11853-11948`).
- `manifest.json` start_url이 `/app` 우회. `app-login.html` 실패 시 날 HTML 오류.
- 서버 함수들이 예외에도 200 + 오류 원문 노출(`reserve:195, ebook-borrow:397, my:151, holding:73, find:207`). reserve 성공 판정이 "완료" 문자열 포함(`:136,170`, 지금은 잠김). 로그인 성공 판정이 `logout` 글자 유무(`semyung_session.ts:92`).
- 학교 이름표: 데이터 꼬리표 전부 `'hankuk'`, 화면은 '세명대학교' — 지금은 기능 오류 없음. 학교 하나 더 붙이는 순간 문제.

---

## 문제 없다고 확인한 것
- 토큰 서명·검증 자체(`_shared/sso_token.ts`), `semyung-my`·`ebook-borrow`·`push-register`는 body 학번 안 믿고 sid로 조회.
- admin-save 비밀번호 서버 비교·op별 컬럼 제한. returnAll·stockLive 시크릿.
- 본문·제목·피드·퀴즈·소감 XSS는 emoji 1곳 빼고 esc 통과. 별이 30초 타임아웃.
- 상세 빠른 전환(loadDesc/loadEbookStock/loadSimilarEbooks/usTulipSearch)은 늦은 응답 버림. 진행바 0나누기 가드. 리스너 누적 없음.
- 전자책 교보/YES24 분기·403 사고 대응 일치. 알림 날짜 KST 일관.
- Supabase 프로젝트·키 파일 간 불일치 없음, 참조 표 전부 존재, 로컬 그림·JS 404 없음, 웹툰 6권 자산 일치.
- International·커뮤니티 메뉴 미노출(사장님 결정대로). WR_MIN 클라·관리자 일치.

## 손볼 순서 (제안 아님, 심각도 순)
1. A1+A2+A3 (뿌리 하나: 신원을 서버가 정하고 활동 쓰기를 토큰 검증 경유로) — 배너 HMAC 서명·`_sso_test.html` 삭제·GET 개발 경로 제거·연계값 재사용 차단
2. A4·A5 (비공개 글 노출, emoji XSS) — 한 줄짜리 수리
3. B1·B2 (데모 책·하드코딩 칸) — 계약 직전 학생 눈에 보이는 것
4. C1 (버전 번호) — 배포 때마다 자동으로 올리는 구조
5. A6 (semyung-ebook 배포 내리기, reserve list 잠금, AI 함수 인증/한도)
6. B4 (학교 접속: semyung-best 캐시, 전자책 세션 몇 분 캐시, 타임아웃)
7. C2 (소감 공개 정책) — 사장님 답 후
8. 나머지 C

---

## 수리 현황 (8/29 저녁 — 커밋 5978585 · 135107e, 실측 완료)
- ✅ A1 학번만 로그인 → 차단(403). 배너 3필드 경로는 `SSO_BANNER_SECRET`+`banner_key` 일치 시만. `_sso_test.html` 삭제
- ✅ A2 활동 표 9종 RLS 본인만(JWT app_metadata.hakbun). 익명 TRUNCATE 등 회수. 좋아요 숫자 PATCH(reviews)는 미변경
- ✅ A3 전교생 목록 → `bookstar_students_public` 뷰(이름 가림). 본체 표는 본인 행만
- ✅ A4 남의 비공개 글 → 조회 조건 + RLS 이중 차단
- ✅ A5 이모지 esc + DB 길이 ≤8 제약
- ✅ A6 semyung-ebook 삭제 · reserve 전부 잠금 · evaluate 로그인 필수 · recommend/curate 400자 · chat 8000자 · byeoli config는 answer만. ⏸ 알라딘 키 소스 박힘·정보나루 http·tulip-cover 무인증·push unsubscribe 소유자·admin 비번 강도 = 미수리
- ⏸ A7 토큰 URL 전달 — 구조 유지(sso_auth는 1회용·재사용 403). 로그아웃 시 서버 세션 종료 추가
- ✅/⏸ A8 로그인 게이트 두 기준·401 처리 — 세션 만료는 refresh 자동, 게스트 승계·이름=학번은 미변경
- ✅ B1 헤일메리·데모 계정 제거 · ✅ B2 사서 담은 책 우선 · ✅ B3 가짜 랭킹·배지 → 실데이터 없으면 칸 미표시
- ⏸ B4 학교 접속량 — 타임아웃 12~15초 추가. semyung-best는 이미 30분 캐시. 전자책 세션 캐시는 학교 합의 후
- ✅ C1 버전 자동 훅(260829b) · BODIES_VER 260827a
- ⏸ C2 소감 공개 정책 — 사장님 답 대기
- ✅ C6 뒤로가기 3건 · ✅ C9 iOS16 lookbehind · ✅ /#mylib 딥링크
- ⏸ C3·C4·C5·C7·C8 나머지 — `_다음세션.md` 남은 것
