# 8/18 세션 — 사장님 8/17 요청 2건 반영 (커밋 3f9aa1d · APP_BUILD 260818a · 배포)

입력: `Desktop\북스타_관리자_엑셀 다운로드 방식_2026.08.17.html`(관리자 5화면 시안) + `Desktop\북스타 수정 요청_2026.08.17.pdf`(39p, 17건)

## 결정(사용자 답)
- 관리자 5화면: 화면 숫자 전부 제거·엑셀 전용 (이유: 이용자 없는데 한눈에 보여주기 X). [결과 확정] 명단 얼리기 UI 제거(함수·op 보존)
- 검색 순서: 우리 도서관 소장 위, 북스타 고전 아래
- 폰 문장선택: 짧게 터치=번역/원문, 길게 눌러 선택=하이라이트·메모·사전·인용
- ⏸ 보류: 인용카드 '공유'(무엇을 공유할지 미정) · 폰 하단 잘림(정확한 증상 미확인 — 탭바 여백만 넣어 둠)
- 챌린지 빌더 미리보기 = 학생 화면 '독서 챌린지' 탭 그대로 / International 아랫줄 = 학생이 고른 언어

## 학생앱 index.html 반영
1. 히어로 "이 달의 추천 도서예요 — 사서" 기본문구 삭제(note 있을 때만 표시) `mlShelf`
2. 4탭 `.pg-intro` 2줄(우리도서관 HTML / 독서챌린지 h1·subtitle 대체 / 세계고전·International은 `renderClassicShelves` clHead — cl-intro·cl-welcome 대체). preview-mode 숨김
3. 이전/다음 2개 → `mpLoadQuiz` 순번 토큰 `_mpLoadSeq`(openViewer가 renderViewer 2회 → mpHydrate 2회 겹침이 원인) + `_mpStepInit` 기존 nav 제거
4. "미션 닫기"→"챌린지 닫기"(2곳) · 완주 팝업 '이 책의 모든 미션 완료' 삭제 · 참여 alert 'Quest Map'→'마이 챌린지'
5. 메모 창: 문서 click 핸들러가 #hlPopup 클릭도 closeNoteBox → 팝업 클릭 제외 + `.hl-note-box` z 130→270(뷰어 200 뒤에 숨어 있었음) + `_fsHost()`에 부착
6. `.bs-cele` z 140→260 (폰 마지막 퀴즈 후 완주 축하가 뷰어 뒤에 숨던 것)
7. 검색: usLibSec 먼저, 고전 뒤 · 폰 `#page-search .book-grid` 3열
8. 폰 헤더: `.main{height:100dvh;margin-top:0;padding-top:header-h}` + `body.hdr-hide .header{translateY(-100%)}` + .main scroll 감시 IIFE · `.page{padding-bottom:84px+safe}`
9. 폰 뷰어 메뉴: `setupImmersiveTap` 빈곳 탭 토글은 탭넘김 모드만 · `scroll` 캡처 리스너(28px 누적, 위=표시/아래=숨김) · 맨 위 당김(touchmove) · `_chromeAutoCheck`(스크롤 불가면 메뉴 유지) · 선택 팝업 폰은 top:10px 가운데
10. 폰 챌린지 쌓기: `.viewer-body.chal-stack`(CSS ≤600) · `_chalStackApply()`(장면카드 있는 책만, mpLoadQuiz에서) · `_mpStepShow` 몸통 top 스크롤(+`_chrIgnoreUntil`) · `_scnSetupSync` 스택이면 skip · 라벨 "오른쪽 —" 숨김 · footer 절대배치(immersive)
- 장면카드 없는 책은 기존 탭(본문/퀴즈·수행)+메뉴 상시 유지(왼쪽이 소설 전문이라 쌓으면 퀴즈가 수십만 px 아래)

## 관리자 admin/index.html (에이전트 작업, 검증 스크린샷 scratchpad shot_*.png)
- 5화면 시안 구조 그대로(1.언제 2.무엇을 → 요약줄 → 엑셀 → note → 경고). 제거 id/함수 목록·보존 함수 목록은 커밋 diff 참고
- 피드관리는 시안대로 챌린지 글 제외(`challenge_id=is.null`) → 챌린지 글 숨김 경로 없음 (사장님 확인 필요)
- 국중 → 미소장/전국 대출/통합검색 표현. 챌린지 빌더 3버튼 + `#chalPv` 인라인 미리보기(학생 renderChalCards 미러) + 순서 바꾸기 모달 `_reMode` 공용화

## 검증
- 구문: index 4블록/admin 2블록 PASS
- 헤드리스 CDP 스모크 `scratchpad/smoke_mobile.mjs`(폰 390×844 + PC): 인트로 4탭·vi 번역·헤더 숨김/표시·검색 순서·3열·뷰어 immersive 스크롤 메뉴(5단계 스크롤 로그)·메모/축하 z·챌린지 쌓기(gb-105: 좌 485+우 693, nav 1개, 다음→2/10 top=0)·PC nav 1개 — 에러 0
- 스모크 함정: 뷰어는 로그인 필수 → localStorage `bookstar-current-student` + `bookstar-joined-chals-<id>` 스텁 필요. 첫 scroll 이벤트는 위치 초기화(dy=0)라 2단계 이상 스크롤로 확인

## 다음
- 사장님 확인 2건(인용카드 공유 정의 · 하단 잘림 증상) + 피드관리 챌린지 글 제외 확인
- 실기기(안드로이드) 확인: 길게 누르기 선택 팝업 위치, 스크롤 메뉴 감도(28px)

## 8/18 리뷰 후속(2차 커밋)
- 학생앱: 별이 추천 카드(버튼 있음)는 폰 2열·키워드 결과만 3열(`.us-grid`) · `_chromeAutoCheck` 자동표시 플래그(dataset.autoChrome — 로딩 중 짧아서 보여준 메뉴는 본문이 길어지면 다시 숨김, 8/14 "열 때 메뉴 없이" 유지) · `.main{scroll-padding-top}` · APP_BUILD 260818b
- 관리자(리뷰어 지적 반영): 미리보기 D-day=학생앱 식(종료 당일 D-1) · 미리보기 탭 라벨=실제 GNB+설명 2줄 · 운영이력/학생글 불러오기 실패 시 빈 엑셀 대신 토스트·중단 · chalAdd 기본기간 오늘~+30일 · 표지 대체텍스트 onerror 인용부호 안전 · 챌린지통계 note(퀴즈=맞힌 문항 수) · 요약 시트에 유형별 합계 차이 안내 한 줄
- 리뷰에서 남긴 것(수정 안 함): ①피드관리 챌린지 글 제외 → 학생앱 상시 글 경로가 없어 사실상 독자 서평 전용 + 챌린지 글 숨김(별 회수) 화면 부재 — 사장님 확인 ②챌린지 순서바꾸기 저장이 학생앱 순서를 보장 안 함(order=created_at.desc, sort_order 없음) — 컬럼 추가 필요 ③화면 note vs 엑셀 NOTE3 문구 미세 차이

## 8/18 후반 — 고전 번역 비용 논의 (결정 대기)
- 사실: 북스타 고전(해외 143권+한국 95권×4언어) 번역+검수에 **약 1,000만원** 소요. 배치 API 50%·초벌 Sonnet/검수 Opus·프롬프트 캐싱은 **이미 적용한 상태**에서 나온 금액 → Claude로 계속 가면 권당 ~4만원 밑으로 안 내려감.
- 판단: 고성능 PC(로컬 30~70B)는 품질이 한 단계 아래라 검수비로 옮겨갈 위험. **1순위=저가 API(DeepSeek/Qwen) 초벌+1차 검수, Claude Opus 최종 1회** → 권당 1만원 안팎 예상. PC는 남은 물량 500권↑이거나 외부 API 금지 조건일 때만.
- 다음: 이미 번역된 짧은 책 1권으로 DeepSeek 파이프라인 파일럿(비용·시간·품질 비교) — 사용자가 책 선택+DeepSeek 키 확인해 주면 스크립트 작성.

## 8/18 아이디어 — 전자책 대출 중(예약 화면)에서 비슷한 책 추천 (사용자 제안, 미착수)
- 위치: 전자책 상세 시트 "예약" 버튼 아래 "지금 바로 읽을 수 있는 비슷한 책" 3~5권(예약이 1순위, 추천은 보조)
- 고르기: 줄거리 임베딩 최근접 + KDC/저자 가산점 + **지금 대출 가능한 전자책만**(실시간 확인) + 시리즈 중복 제거, 3권 미만이면 종이책 소장까지 확장
- 카드 클릭=바로 대출. 측정: bookstar_events origin='similar' → 관리자 엑셀에서 추천→대출 전환 확인
- 작업량 반나절 추정. 재료(줄거리 92%·임베딩·대출 실배선) 이미 있음

---

# 8/18 오전 세션 — 줄거리 재임베딩 완주 + 추천 설계 확정 (커밋 4610a9f)

## 1. 줄거리 재임베딩 (완료)

**문제**: 8/9~8/18 백필로 채운 줄거리 18.4만 건이 **임베딩에는 안 실려 있었다.**
임베딩은 8/6~7에 만들어졌고 그때 종이책 줄거리는 0.07%였다. 의미검색이 제목·저자만 보고 있었던 것.

**사용자 제안**: "기존 임베딩 전부 삭제하고 다시" → **삭제 없이 제자리 UPDATE로 실행**
1. 비우는 UPDATE가 재임베딩과 똑같이 비싸다(HNSW 실린 표는 행당 449ms) — 같은 일을 두 번
2. 비운 동안 의미검색이 0건이 된다
3. 줄거리 없는 9.2만 행은 텍스트가 같아 같은 벡터가 나온다(재계산 무의미)
- 사용자의 진짜 걱정("세대가 섞여 지저분")은 실측으로 해소: `embed_text` 빈 행 **0건**, 차원 1536 단일
  = 이미 한 세대. 제자리 덮어쓰기 결과는 삭제 후 재생성과 동일

**실행**
- `tulip_sync.py --reembed` 신설. 대상=줄거리가 embed_text에 없는 행, ctrl 커서 페이징이라 중단해도 이어서
- 레시피를 `_embed_text()` 한 곳으로 모음(신규·재임베딩이 같은 문자열을 만들어야 세대가 안 섞임)
- **HNSW(2.1GB) 먼저 DROP** → 쓰기 449ms→13ms / 청크 4스레드 → **260행/분 → 2,100행/분**
- 18만 건 80분 → Micro→**Large 승급, pg_cron으로 HNSW 재빌드 13분** → Micro 복귀 → `vacuum analyze`

**결과**: 임베딩 행 298,811 유지(유실 0) · 줄거리 실린 행 **22,625 → 206,638** · 미반영 1건
· 인덱스 2,115MB 재생성 · 비용 OpenAI $1 안쪽 + Large 30분
**검증**: "마음이 지치고 위로가 필요할 때"→당신의 마음을 안아줄게요 / "조선시대 여성의 삶"→조선이 버린 여인들·조선의 여성들(상위 5권 전부 정확) / 기후위기·통계실습도 정확

**함정 3개**
- 🔑 **판별식을 저장 방식과 맞춰라** — `esc()`가 제어문자를 공백으로 바꾸므로 줄바꿈으로 시작하는
  줄거리 5건이 영원히 "미반영"으로 잡혔다. 검사식에 `regexp_replace(...,'[[:cntrl:]]+',' ')` 추가
- 🔑 **TaskStop은 셸만 죽이고 python 자식은 살아남는다** — 옛 코드로 도는 유령 프로세스가 같은 일을
  중복 실행 중이었다(OpenAI 비용 이중). 장시간 작업은 `Start-Process -WindowStyle Hidden -PassThru`로
- 🔑 **pg_cron의 statement_timeout 해제는 cron 명령의 별도 문장으로.** 함수 안 `set local`은 무효 —
  120초 시계는 바깥 호출(`select f()`)에 이미 걸려 있어 CREATE INDEX가 매번 정확히 120초에 잘린다.
  진단은 `cron.job_run_details`(status·return_message)로. `pg_stat_activity`만 보면 매분 재시작을 진행 중으로 오인
- 인스턴스 크기 변경 = **PATCH** `/v1/projects/{ref}/billing/addons` `{"addon_type":"compute_instance","addon_variant":"ci_large"}` (POST·PUT은 405)

## 2. 추천 설계 확정 (대화로)

**엔진 하나 + 화면별 다이얼 3개**(인지도 가중 / 대출가능을 필터냐 정렬이냐 / 표지·줄거리 필수 여부)

공통 규칙
1. 관련성으로 후보(줄거리 임베딩 + 어휘)
2. **도장 찍힌 책만 제외** — `''`(조회했는데 없더라)만. `null`(미조회)·**신착은 통과**
   (실측: 줄거리 도장 36,751 / null 54,500 · 표지 도장 26,510 / null 54,498)
3. **시리즈·판본 중복만 대표 1권**, 같은 저자는 빼지 않고 상한만 (사용자 지적으로 교정)
4. 인지도 정렬 5. **0건이면 조건 풀고 재검색**
- **특정 도서 지목 검색은 어디서도 필터 없음** — 소장한 책을 "없다"고 하면 도서관 목록의 신뢰가 깨진다
- 별이에서 대출가능 하드필터 금지 — "있습니다, 지금 대출중이라 예약 가능"이 정답

| | 대체재(급류 없을 때) | 별이 AI | 발견·홈 진열 | 사서 큐레이션 |
|---|---|---|---|---|
| 인지도 | 최대(주 정렬키) | 질문이 막연할수록 세게 | 전국 인기+큐레이션 | 중간 |
| 대출가능 | 필터 | 표시만·정렬 뒤로 | — | 무관 |

## 3. 🔑🔑 측정 — 대학도서관 ≠ 공공도서관 인기

세명대 **대출중 2,069종(8/14 전수 스냅샷)** vs 정보나루 인기대출 목록:
- top 1,000 **3.4%** / top 5,000 **8.7%** / 확장 45,149종(기간·20대·KDC 0~9) **22.6%**
- 나머지 1,601종 = 이방인·주역과의 산책·SAS 통계분석·Hamlet·Norton Anthology·Rhino 3D
  = 전공 교재·수업 지정도서·고전. **줄거리 보유율 81%**(필터 걸어도 대부분 생존)
- 정보나루 인기 API 실측: `loanItemSrch` 기간당 5,000 + `age=20` 5,000 + `kdc=0~9` 각 5,000
  (`dtl_kdc=33` 중분류 동작, `dtl_kdc=3`은 0건). pageSize 200. 45,149종 수집에 300여 호출

**단, 그 데이터를 발견 화면에 쓰면 안 된다** (사용자 지적):
위 77%는 수요가 아니라 **"검색창밖에 없는 도서관 홈페이지가 걸러낸 결과"**다. 찾을 게 있는 사람만 오니
전공서만 남는다. 증거 = 밀리 교차(8/14): 학생들이 밀리에서 읽은 책의 73~77%를 도서관이 이미 소장.
같은 학생이 브라우징 환경에선 소설·에세이를 읽었다 → **학교 과거 대출 데이터를 발견 화면 주 신호로 쓰면
과거 편향을 재생산.** 세명대 대출 신호는 전공·주제 질의에서만.
(내가 한 번 "loan_rank를 주 신호로"라고 제안했다가 이 지적으로 철회)

## 4. 언어별 대출 실측 (8/14 스냅샷, 종이책)

| 언어 | 소장 | 대출중 | 대출률 |
|---|---|---|---|
| 독일어 | 4,154 | 53 | **1.28%** |
| 한국어 | 242,695 | 1,930 | 0.80% |
| 영어 | 18,500 | 88 | 0.48% |
| 일본어 | 2,699 | 12 | 0.44% |
| 중국어 | 4,570 | 2 | **0.04%** |
| 스페인·프랑스 등 | ~1,000 | 0 | 0% |

⇒ 외서 일괄 배제는 틀렸다(독·영은 실수요). **중국어·소수언어만 후순위.**
진짜 답은 배제가 아니라 **전공별 노출**(SSO가 학과를 주므로 가능)

## 5. 신착 결손 점검 (사용자 우려 → 실측)

최근 30일 등록 68권 중 표지 67·줄거리 63. 빠진 5권은 전부 외서(영1·일4)이고
**정보나루·알라딘 양쪽 다 줄거리 0자** — 우리 파이프라인 누락이 아니라 국내 DB의 한계.
표지 1권은 알라딘에 상품 자체가 없음. `開放系の…`는 알라딘 표지가 noimg라 우리 가드가 걸러낸 것(정상 동작).
7월 입고 328권 중 외서 9권(2.7%)뿐이고 그 5권은 건축·디자인·도서관학으로 주제가 묶임 = 학과 신청/사서 선정으로 보임.

## 6. 남은 것

- **`semyung_loan_rank` 76행뿐·8/4 이후 갱신 멈춤** (수집 작업 `Bookstar_Semyung_Sync` 03:50 점검 필요)
- 인기 점수 표 `book_popularity`(isbn13·점수·출처·갱신일) — semyung_tulip에 컬럼 달지 말 것
- 신착 결손 감시(최근 30일 표지·줄거리 빠진 수 매일 집계)
- 신착의 줄거리가 나중에 채워지면 재임베딩 필요 — 데일리에 `--reembed` 소량 붙일지 미결

---

# 8/18 오후 세션 — 추천 v2 설계·적용 + 고전 임베딩

## 논의(사용자와 확정)
- 도서앱 = **매체**(밀리: 열기→읽기). "도서관 가세요/예약하세요"가 트래픽 누수점. 학술정보원=도구(찾기) / 도서앱=매체(읽기) — 하나로 관리하니 학생이 책에 접근 못 함(진단서 프레임)
- 대출중 8/14 스냅샷 2,127종 KDC 분해: 전공 81%(건축 323·법 106·언어 216·컴퓨터 186…) / 교양 ~400 = 최근 5년 한국소설·에세이·일본추리·영미SF·실용심리·고전 = 전국 대중취향과 동일 → 발견·대체재 인기신호는 전국 것 OK. 신착은 즉시 대출됨
- 세명대 구독 이력: 2025 밀리 6개월 → 2026 크레마클럽(첫). "크레마 끊고 전자책 구매" 제안은 접음(북스타료가 자료구입비와 경쟁·구매형은 잠김·화제작 매년 교체)
- **최종 모델 "울타리 넓이만 다르다"**: 관련성=울타리, 인기=순서. 대체추천 울타리=장르(줄거리X) / 별이=질문 / 사서=좁게. 별이 5권.

## 적용 (Edge Fn 배포 + klever_demo e1b7808·b5c4114·adfc7c8)
- byeoli-search caps 5·재고 즉시읽기 정렬·대출중 1권 예외·시리즈 dedup·형태 병합·리랭킹 diag
- curate similar 모드 + similar_ebooks RPC + _shared/ebook_stock.ts
- index.html: 별이 카드 _avail 라벨, 상세 대출중→대체 3권(data-origin=similar), 고전 카드(openDetail)
- **classic_embeddings 310권 + match_classics** → 별이 4번째 소스
- 라이브 스모크: 대체추천 3권(천 개의 파랑·작별하지 않는다·칵테일 러브 좀비) 렌더 확인

## 🚨 발견
- **Anthropic API 크레딧 소진** (rerank diag: "credit balance is too low") → 사용자 충전 필요. 그 전엔 검수·분류·답변 전부 fail-open

## 8/18 저녁 후속
- Anthropic 크레딧 충전 → 새 키 두 프로젝트 배포 → 리랭킹·답변 복구 확인. OpenAI 키 교체·배포, Gemini 키 교체(hwik-web/.env로 이관, 표지 스크립트 16개 재지정, 이미지 모델은 503 일시과부하)
- 대체추천 카드 → 상세 경유(93f2f51, 260818d) 헤드리스 확인
- 전자도서관 예약 = 반납 시 자동 대출(한도 초과 시 예약자우선기간 직접 대출). 안내 문구/알림 택일 미결
- 별이 게이트 오탐 1건 발견(classics 뒤집기 조건 추가 예정)

## 8/18 밤 — 사장님 수정요청 PDF 5건 (Desktop\북스타 수정 요청_2026.08.18.pdf) → 전부 반영·라이브 (d606479~, APP_BUILD 260818g)
1. PC 세계고전·International 소개글 위치 = 우리도서관·독서챌린지와 동일 — `#page-collection{padding-top:64px}` 삭제(예전 제목 삭제 시 넣은 여백). 헤드리스 실측 4탭 top 98px 동일
2. 모바일 랭킹(세명대 인기 전자책·우리 학교 대출) = 밀리 방식 3권씩 한 열, 옆으로 넘김 — `.ml-rgrid` max-width 559 미디어: grid-auto-flow column·3행·열폭 min(340px,100vw-88px)·snap. ⚠️기본 `grid-template-columns:1fr`이 첫 열을 눌러 제목이 잘렸음 → `none`으로 해소
3. 인용 카드 '공유' 버튼 삭제(이미지 저장만). shareQuoteCard 함수는 미사용 잔존
4. 미션 패널(DB 퀴즈) 책(귀향 gb-122 등)은 퀴즈 진행이 Supabase에만 저장되고 내 챌린지 카드는 로컬 캐시(bookstar-chal-…)만 읽어 '퀴즈 0/0' → `_mpMirrorLocal()`로 답·quiz_total·mp_done을 로컬에 미러(답할 때·하이드레이트·완주), `_chalBookScore` total 폴백=quiz_total, `_chalBookState` done=mp_done, mpSaveProgress에 quiz_ok/score 동봉, closeViewer 끝에 renderQuestMap(). ⚠️읽기 %는 challenge 모드에서 원래 집계 안 함(v4Recalc full 한정) — 그대로 둠
5. 폰 선택 툴바(하이라이트·메모·사전·인용카드) 화면 하단 고정 — `.hl-popup-bottom`(bottom:14px+safe-area). 브라우저 기본 메뉴는 웹에서 못 없앰(사용자 확인)
스모크: scratchpad/ui_smoke_fix5.mjs(5건 전부 통과) + shot_fix5.mjs(모바일 랭킹 스크린샷)
