# 다음 세션 — 세명대 표지·줄거리 백필

시작 문장: **"백필 이어서"**
작업 폴더: `cd Desktop\북스타\klever_demo`

---

## 한 줄 요약
종이책 27.3만 권의 **표지·줄거리를 정보나루 API로 채우는 중.** 코드는 다 끝났고
**매일 새벽 3:30 자동 실행**된다. 6일이면 완료. **사람이 할 일은 없다.**

---

## 지금 상태 (2026-08-10 07:30 기준)

| | 값 |
|---|---|
| 표지 | **173,674** / 272,991 |
| 줄거리 | **10,872** / 272,991 |
| 남은 대상 | 201,610건 (표지 38,683 + 줄거리 162,927) |
| 예상 완료 | **약 6일** (8/16 전후) |

- 스케줄 작업 `BookstarTulipSync` = 매일 03:30, 다음 실행 8/11 03:30
- 하루 3만 건 처리에 **약 3.8시간** 소요 (03:30~07:20)
- **최신 발행 순**이라 2020년대 책이 먼저 채워진다 → 이틀이면 최근 책은 대부분 완료

### 진행 확인 방법
```powershell
node "<scratchpad>\progress.mjs"          # 또는 아래 SQL
```
```sql
select count(*) filter (where cover_url is not null and cover_url<>'') 표지,
       count(*) filter (where description is not null and description<>'') 줄거리
from semyung_tulip where kind='paper' and mat_type='m';
```
로그: `logs\tulip_daily.log`

---

## 데이터 소스 (2026-08-09 전환)

**1순위 = 도서관 정보나루** `data4library.kr/api/srchDtlList?authKey=..&isbn13=..&format=json`
- 줄거리 확보 **94~97%**, 하루 **30,000**
- 키: `b6b219379c6c9809d2254684e51feed41837c4e034a0c36d40a54ef9253dcad9`
- ⚠️ **IP 등록 필수** — 미등록이면 하루 500건에서 `errCode=outOflimit`.
  현재 사장님 PC(1.235.111.158) 등록됨. **PC나 IP가 바뀌면 data4library.kr에서 재등록**
- ⚠️ `srchBooks`에 `isbn=`를 쓰면 파라미터가 무시돼 **엉뚱한 책**이 온다. `srchDtlList` + `isbn13` 필수
- ⚠️ `errCode`는 한도만이 아니라 "ISBN에 해당하는 도서가 없습니다"(정상 미보유)에도 붙는다.
  **`outOflimit`만 한도로 판정할 것**

**2순위 = 알라딘 폴백** — 확보 70~77%, 하루 **5,000 하드 한도**

**표지 화질**: 정보나루는 썸네일 주소(`/cover/`)로 주므로 알라딘 CDN이면 `/cover500/`으로 승격
(18KB → 87KB, 실측 10/10 성공). 네이버 URL은 이미 원본이라 손대지 않는다.

---

## 대체 소스는 전부 조사 끝 — 다시 찾지 말 것

| 후보 | 결과 |
|---|---|
| 네이버 책 검색 API | **2026-07-31 종료.** 같은 키로 blog/news/kin은 200인데 book·shop만 404 SE05 |
| 네이버 쇼핑 책 카탈로그 | 서버 요청 418(WAF), 헤더 우회해도 **로그인 페이지**가 온다 |
| 도서관 서버 `/openapi` | 표지 URL만. 초록·목차 빈값, 미문서 엔드포인트 9종 전부 404 |
| 한국어 위키백과 | **1.7%** (60권 중 1건, 그마저 저자 인물 문서) |
| 네이버 백과사전 | 살아있지만 백과 항목이지 책소개가 아님 |
| Google Books | 키 없으면 공용 할당량 소진(429). 키 받아도 1,000/일 |
| 국중 seoji | `BOOK_SUMMARY`·`BOOK_INTRODUCTION_URL` 빈값 |
| **알라딘 프리미엄 신청** | **하지 않기로 결정(8/10)** — 아래 참고 |

### 알라딘 프리미엄을 안 하는 이유
신청서 조건 중 두 개를 못 지킨다.
1. **"타 인터넷서점 연결 없이 알라딘만 단독 연결"** — 북스타는 교보 전자책·YES24 크레마
   링크가 서비스의 핵심이라 뺄 수 없다
2. **"도서관/공공기관 명의만, SI구축개발업체 명의 불가"** — 우리는 업체
3. 추가로 "알라딘 구매하기 버튼 노출" 필요 → 도서관 앱에 서점 구매 버튼은 부적절

실익은 6일 → 3.5일(2.5일 단축)뿐. **세명대 정식계약 후 도서관 명의로는 재검토 여지 있음.**
현재 기본형은 조건 준수 중(앱 하단에 "표지·별점 알라딘 · 대출·키워드 정보나루" 출처 표기 있음).

---

## 성능 — 더 줄일 수 있는지 이미 다 따져봤다

**달력 6일은 API 한도가 정한 것이라 못 줄인다.** (정보나루 3만 + 알라딘 5천 = 하루 3.5만)

DB 쓰기가 느린 원인도 규명 완료:
- 같은 UPDATE인데 **embedding 있는 행 449ms/건 vs 없는 행 13ms/건 = 34배**
- 범인은 `idx_tulip_emb_paper`(HNSW). description만 바꿔도 행 재작성 → 인덱스 갱신
- 1GB 마이크로 인스턴스라 이게 지배적
- **인덱스를 내리면 하루 3.8시간 → 7분**이 되지만 **달력은 6일 그대로**(API 한도 때문).
  재빌드에 인스턴스 임시 업그레이드가 필요하고 과금·검색품질 영향이 있어 **하지 않기로 결정**

조회는 이미 최적화됨: 8스레드 병렬(372ms → 51ms). 정보나루는 동시 24도 받아준다(16ms).

---

## 백필 끝나면 할 일

1. **표지 핫링크 감시 장치 추가** (사용자와 합의됨, 백필 완료 후)
   - 우리가 저장하는 건 이미지가 아니라 **알라딘·네이버 URL**이다. 정보나루도 이미지를 호스팅하지 않는다
   - 2026-08-10 실측: 레퍼러 없음/bookstar/엉뚱한 사이트 **전부 정상** = 아직 안 막힘
   - 자체 저장 시 **9.3GB**(19.5만 장 × 50KB) + 전송비 → 이용자 0인 지금은 불필요
   - 대신 **매일 표지 100장 샘플 검사 → 실패율 급등 시 알림**을 데일리에 추가 (비용 0)
2. **재임베딩 검토** — 지금 채워지는 줄거리는 검색(별이 AI)에 반영되지 않는다.
   임베딩이 이미 만들어져 있기 때문. 백필 끝날 무렵 전체 재임베딩 1회(약 $0.3) 권장

---

## 이번 세션에 고친 것 (커밋)

| 커밋 | 내용 |
|---|---|
| `c49ac6c` | 데이터 오염 2종 수리(표지 이중스킴 147·설명 엔티티 1,638) + 재발 코드가드 |
| `fe1ff66` | 표지 백필 15배 — ctrl 정렬이 성능을 죽이고 있었음(7.5%→무작위 50%), eBook 재조회 0/111 낭비 제거 |
| `49f24da` | 표지 없는 책 대체표지 시안 4종 |
| `9e71274` | **A안 활자 표지 8만 권 적용**(호출부 10곳). 검색결과가 까만 사각형만 뜨던 버그 동시 수리 |
| `50329f8` | 줄거리 백필 사각지대 16.2만 권 편입(표지만 있고 줄거리 없던 책을 아예 안 보고 있었음) |
| `0b20fd3` | 줄거리 백필 최신 발행 우선 |
| `3add544` | **정보나루 전환** (52일 → 8일) |
| `b5b64c8` | 대상 쿼리가 15분 넘게 안 끝나 데일리가 멈추던 문제 (union+정렬키 → 두 풀 분리, 2.3초) |
| `9cbfd7f` | 조회 8스레드 병렬 + 쓰기 values 조인 1문 |
| `91d9e2b` | 데일리 로그 `python -u` (버퍼링돼 진행 중인데도 멈춘 것처럼 보였다) |

### 스케줄 작업도 고쳤다
`3221225786`(=Ctrl+C 강제종료)로 죽어 있었다. 원인 설정 3개를 껐다.
- `StopOnIdleEnd` True→False (**PC를 만지면 작업이 죽는다**)
- `StopIfGoingOnBatteries`, `DisallowStartIfOnBatteries` True→False
- 추가: 실패 시 20분 간격 3회 재시도, 실행시간제한 72h→12h

---

## 이번에 얻은 교훈 (같은 실수 반복 금지)

- **배치 쿼리는 실전 limit으로 시간을 재라.** limit 200~300 테스트는 33,860에서 터지는 걸 못 잡는다
- **에러 문자열로 성패를 판정할 땐 실제 응답을 눈으로 먼저 봐라.** 정보나루 한도 메시지엔
  '제한·초과·한도'가 하나도 없어서(요청·등록뿐) 차단 271건을 "빈 응답"으로 오분류했다
- **비교용 정규화가 원본을 훼손하지 않는지 확인하라.** strip()이 책 제목의 `<다크 플레이스>`를
  태그로 보고 지워서 "알라딘이 더 짧다"는 잘못된 결론이 나올 뻔했다
- **로그가 안 찍힌다고 멈춘 게 아니다.** `>>` 리다이렉트 시 파이썬 stdout은 블록 버퍼링된다
- **대량 UPDATE는 `right(ctrl,1)` 10분할.** 4,435행도 통짜면 서버 statement timeout(57014)
