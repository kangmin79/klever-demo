# 다음 세션 — 세명대 표지·줄거리 백필

시작 문장: **"백필 이어서"**
작업 폴더: `cd Desktop\북스타\klever_demo`

---

## 한 줄 요약
종이책 27.3만 권의 **표지·줄거리를 정보나루 API로 채우는 중.** 코드는 다 끝났고
**매일 새벽 3:30 자동 실행**된다. 6일이면 완료. **사람이 할 일은 없다.**

---

## 🏁🏁 8/11 표지 자체 저장 **완주** — PC 4.6GB + 클라우드 서빙까지 하루 만에
- **PC**: `바탕화면\북스타\데이터\표지\` **187,805장 실물**(99.5%) + 실패마커 970(0바이트=원본 깨짐). 4.6GB
- **클라우드**: `covers` 버킷 **187,598장**, 무작위 10/10 서빙 검증. 잔여 수백 장은 내일 데일리가 자동 회수
- **서빙 주소 규약**: `…/storage/v1/object/public/covers/<ctrl>.webp` — **DB 컬럼 불필요**(주소를 규약으로 계산, 404면 원본 cover_url 폴백→활자표지). 앱·웹 전환은 이 순서만 넣으면 끝
- 🔑🔑**속도 전쟁의 진범 = semyung_tulip UPDATE**(임베딩 행 재작성+HNSW 0.45s/행). 1.4건/초의 원인이었고, 스토리지·네이버는 무죄(내 스토리지 오진 1회 정정). **DB 무접촉 설계로 66~87건/초**: 상태=디스크 파일, 실패=0바이트 마커, 푸시 추적=홀쭉한 `mirror_pushed`(ctrl) INSERT. ⚠️**임베딩 실린 표에 대량 마킹 금지** — 진행 추적은 좁은 표/파일시스템으로
- ⚠️`cover_local` 컬럼은 초기(~1.4만)만 채워진 유물 — 신뢰하지 말 것. 진실=디스크·mirror_pushed
- 스케줄 `BookstarCoverMirror`(07:00, 우선순위 5로 상향 — 7이면 윈도우가 깎아 19건/초): 질주→pull→덤프→푸시 순. 신착 하루 십수 장 자동

## 🖼️ (기록) 8/11 표지 미러링 최초 설계 — 핫링크 의존 제거
- **`scripts/cover_mirror.py` + 스케줄 `BookstarCoverMirror`(매일 07:00, 예산 6만/일)** — 커밋 a3aaf96
- 원본→**최대 600px WEBP q85**(사장님 지시로 300→600 상향, 3배 밀도 폰까지 선명)→Storage `covers` 버킷→`cover_local` 컬럼. **cover_url 출처는 절대 안 지움**
- 파일럿 120/120 + 공개 URL 서빙 실측 8/8 진짜 WEBP. 총 ~19만 장 = **3~4일**
- 서빙 주소: `https://gkujptyfrzqrjrvovbnc.supabase.co/storage/v1/object/public/covers/<ctrl>.webp`
- 🔜 완료 후: 앱·웹 표지 로딩을 cover_local 우선(→원본 폴백→활자표지)으로 전환 + 신착 일일 미러는 자동
- 진행 확인: `select count(*) from semyung_tulip where cover_local<>''` / 로그 `logs\cover_mirror.log`

## 🏁 8/11 이정표 — **표지 백필 종료(풀 소진), 이제 줄거리만**

| | 값 |
|---|---|
| 표지 | **188,775** (+10,338 — 시도 풀 잔여 0, 천장 도달) |
| 줄거리 | **33,267** (+17,750) |
| 남은 것 | **줄거리 155,886뿐** ≈ 5일 (8/16 예상 유지) |

- 밤새 실행 03:30~06:19 정상 종료(exit 0). 예산이 표지→줄거리로 자동 전환됨(설계대로)
- 🐛`쓰기 실패(200건) HTTP 400` × 2 발견 → **수리(21bc96d)**: esc()에 서로게이트 제거 + 실패 배치 반쪼개 재시도. 유실분은 익일 자동 재조회라 데이터 손실 없음. 소량 실동작 확인(28/30, 오류 0)

## (지난 기록) 상태 (2026-08-10 09:26, 오늘치 완료 후)

| | 값 |
|---|---|
| 표지 | **178,437** / 272,991 |
| 줄거리 | **15,517** / 272,991 |
| 남은 대상 | 181,954건 (표지 18,683 + 줄거리 163,271) |
| 예상 완료 | **약 5.6일** (8/16 전후 — 일정 유지) |

- 스케줄 작업 `BookstarTulipSync` = 매일 03:30, 다음 실행 8/11 03:30. **오늘 LastTaskResult 0(정상 종료)**
- 설정 확인 완료: `MultipleInstances=IgnoreNew` · 배터리 중단 False · `StopOnIdleEnd` False · 12h 제한 · 실패 시 20분×3회 재시도
- **최신 발행 순**이라 2020년대 책이 먼저 채워진다 → 이틀이면 최근 책은 대부분 완료

### 2026-08-10 실적 — **표지 +16,033 · 줄거리 +14,412** (오늘 실행 두 번 합계)

| 실행 | 표지 | 줄거리 | 표지실패('' 마킹) |
|---|---|---|---|
| 03:30 (중단됨) | 7,602 | 6,213 | 4,936 |
| 07:32 (정상 종료) | 8,431 | 8,199 | 18,769 |
| **오늘 실행 전** | 162,404 | 1,105 | — |
| **현재** | **178,437** | **15,517** | — |

⚠️**앞 문서의 "07:30 기준 표지 173,674"는 03:30 실행이 돌아가는 중에 잰 값이라 기준으로 쓰면 안 된다.**
그걸 기준 삼으면 오늘 성과가 +4,763으로 3분의 1로 축소돼 보인다.
**성과는 `updated_at` 구간으로 세야 한다**(서버 시각은 **UTC**, KST=UTC+9 — 07:32 KST = `2026-08-09 22:32+00`).

두 가지가 "고장처럼 보이지만 정상"이다. 아래 두 절에 근거를 남긴다.

### 진행 확인 방법
```powershell
node "<scratchpad>\progress.mjs"          # 또는 아래 SQL
```
```sql
select count(*) filter (where cover_url is not null and cover_url<>'') 표지,
       count(*) filter (where description is not null and description<>'') 줄거리
from semyung_tulip where kind='paper' and mat_type='m';
```
로그: `logs\tulip_daily.log`

---

## "알라딘 0/4,200"은 버그가 아니다 — 남은 표지 풀이 외서라서 (2026-08-10 실측)

로그 마지막 줄에 `출처 정보나루 9,240 · 알라딘 0 | 호출 나루 28,000 · 알라딘 4,200`이 찍힌다.
알라딘 4,200회를 쓰고 한 건도 못 건졌다는 뜻이라 키 만료·ISBN10 버그를 의심하기 쉬운데,
**실패 ISBN 12건을 직접 조회해 보니 전부 정상 응답에 미보유**였다. 표본이 이렇다:

```
9783839460924 Schriftlichkeit : Aktivitat...   (독일어)
9781135994716 Contemporary landscapes...        (영어)
8985897004    (國譯)鄕藥集成方 上卷              (1989 한적)
9787537743433 醫案醫話入門必讀 3                 (중국어)
```

표지가 아직 없는 책은 **국내 서점·도서관 DB에 애초에 없는 학술 외서**로 수렴한다.
오늘 처리분의 언어별 표지 확보율:

| 언어 | 확보율 | 남은 풀 |
|---|---|---|
| **kor** | **51%** | 12,006 |
| eng | 10% | 3,938 |
| jpn | 6.8% | 342 |
| chi | 1.5% | 830 |
| ger | 0.9% | 1,290 |
| spa·pol·fre | 0~3% | 172 |

**조치하지 않기로 한 이유**: 비한국어를 뒤로 미루면 예산 6,677회(=0.2일)를 아낄 수 있지만,
남은 표지 풀 자체가 18,683건(0.6일)뿐이라 **8/11 하루면 어차피 마르고 그 뒤 예산은 전부
줄거리 풀로 흐른다.** 이번 주에 두 번 터진 배치 쿼리를 0.2일 때문에 다시 건드릴 값어치가 없다.
줄거리 풀 163,271건은 **98%가 한국어**(160,062)라 알라딘 폴백도 8/11부터 다시 제 몫을 한다.

## 로그의 `줄거리 0/0`은 "줄거리를 못 채웠다"는 뜻이 아니다

두 가지가 겹쳐 있다.

1. **pri1(줄거리 전용) 풀은 실제로 0건 처리됐다** — pri1이 굶은 게 아니라 **pri0(표지) 풀이 예산
   전량을 먹었기 때문**이다. `take = (28,000+4,200)×1.05+50 = 33,860`인데 pri0 풀이 그보다 컸다.
   표지 풀이 마르는 8/11부터 자동으로 줄거리로 넘어간다.
2. 🔑**그런데 줄거리는 오늘 14,412건이 채워졌다.** 정보나루가 표지와 줄거리를 **한 번에 주므로**
   pri0 책이 표지를 얻을 때 줄거리도 같이 들어온다(`description=coalesce(nullif(v.d,''), …)`).
   **로그의 `dhit/dmiss` 카운터는 `pri==1` 행만 세도록 돼 있어 이 14,412건이 안 잡힌다.**
   → 카운터를 실적으로 읽지 말 것. 실적은 DB `updated_at`으로 센다.

**손댈 것 없음** — 표시만 실제보다 짜게 나올 뿐 동작은 정상이다.

## 적재 감사 — 언제든 다시 돌릴 것 (2026-08-10 신설)

```powershell
python -u scripts\audit_tulip.py --since 2026-08-09T18:30   # 그 이후 갱신분 (⚠️UTC, KST-9h)
python -u scripts\audit_tulip.py                            # 전량
```

**A 형식**(전량·비용0) / **A-2 오염의심**(같은 줄거리 + 무관한 제목) / **B 표지 실물**(표본, 매직바이트·크기·해시중복) / **C 오매칭 교차검증**(도서관 제목 vs 정보나루 제목).

### 8/10 감사 결과

| | 결과 |
|---|---|
| A 형식 (40,392행) | **0건** — 이중스킴·noimg·엔티티·태그·제어문자 전부 0 |
| B 표지 실물 (25장) | **25/25 진짜 이미지**, 해시 중복 0 |
| C 오매칭 교차검증 | ⏸**못 함** — 정보나루 일 한도 소진. **8/11 이후 재실행 필요** |

**찾아서 고친 것 172건**

1. **판매 안내문이 줄거리 자리에 170건** — 책 내용이 0%다
   - `[해외주문원서] … 주문취소 및 반품이 불가합니다` **71건**(종이책, 알라딘 상품문구)
   - `★ 책의 특징 ★ … 슬리피 할로우의 전설 오디오북 첨부` **99건** — 「간도를 등지면서」
     「어촌점묘」 같은 **한국 근대 단편에 딴 책 안내가 붙어 있었다**
   - 뒤에 실제 본문이 이어지는 6건은 **보존**(템플릿 앞부분만 잘라냄)
2. **표지 URL 2건이 깨져 있었다** — `.../cover500/i 지속가능한 관광의` 처럼 **책 제목이 URL에 섞임**.
   `norm_url`이 호스트만 검사해서 통과시켰다. 앱에선 그냥 빈칸이 된다

**코드가드 2개 추가**(단위테스트 10/10): `norm_url`=URL에 raw 공백 있으면 거부 /
`_desc`=판매 안내문 거부 + 앞 템플릿 절단

### 🔑 이 감사에서 배운 것

- **낱말로 쓰레기를 판정하지 말 것.** '절판·배송·이벤트·사은품'으로 걸러 200건이 나왔는데,
  눈으로 보니 **130건이 멀쩡한 책소개**였다("오랫동안 절판되었다가 다시 펴낸 책"). 그대로 지웠으면
  멀쩡한 데이터를 날릴 뻔했다. 진짜 쓰레기는 2종뿐이었다
- **중복 줄거리 자체는 정상이다** — 시리즈 공통 설명이다(삼국지 120권·世界哲學史 4권).
  오염은 **'같은 줄거리인데 제목이 서로 무관한'** 것뿐이다. 그게 A-2 검사다
- **A-2가 잡은 것도 다 오염은 아니다.** "무역 전공자를 위해 이론을 정리하였다" 같은 **분야 일반 설명**은
  틀린 정보가 아니라 뭉뚱그린 것 → **손대지 않는다**

## 데이터 소스 (2026-08-09 전환)

**1순위 = 도서관 정보나루** `data4library.kr/api/srchDtlList?authKey=..&isbn13=..&format=json`
- 줄거리 확보 **94~97%**, 하루 **30,000**
- 키: `b6b219379c6c9809d2254684e51feed41837c4e034a0c36d40a54ef9253dcad9`
- ⚠️ **IP 등록 필수** — 미등록이면 하루 500건에서 `errCode=outOflimit`.
  현재 사장님 PC(1.235.111.158) 등록됨. **PC나 IP가 바뀌면 data4library.kr에서 재등록**
- ⚠️ `srchBooks`에 `isbn=`를 쓰면 파라미터가 무시돼 **엉뚱한 책**이 온다. `srchDtlList` + `isbn13` 필수
- ⚠️ `errCode`는 한도만이 아니라 "ISBN에 해당하는 도서가 없습니다"(정상 미보유)에도 붙는다.
  **`outOflimit`만 한도로 판정할 것**

**2순위 = 알라딘 폴백** — 확보 70~77%, 하루 **5,000 하드 한도**

**표지 화질**: 정보나루는 썸네일 주소(`/cover/`)로 주므로 알라딘 CDN이면 `/cover500/`으로 승격
(18KB → 87KB, 실측 10/10 성공). 네이버 URL은 이미 원본이라 손대지 않는다.

---

## 대체 소스는 전부 조사 끝 — 다시 찾지 말 것

| 후보 | 결과 |
|---|---|
| 네이버 책 검색 API | **2026-07-31 종료.** 같은 키로 blog/news/kin은 200인데 book·shop만 404 SE05 |
| 네이버 쇼핑 책 카탈로그 | 서버 요청 418(WAF), 헤더 우회해도 **로그인 페이지**가 온다 |
| 도서관 서버 `/openapi` | 표지 URL만. 초록·목차 빈값, 미문서 엔드포인트 9종 전부 404 |
| 한국어 위키백과 | **1.7%** (60권 중 1건, 그마저 저자 인물 문서) |
| 네이버 백과사전 | 살아있지만 백과 항목이지 책소개가 아님 |
| Google Books | 키 없으면 공용 할당량 소진(429). 키 받아도 1,000/일 |
| 국중 seoji | `BOOK_SUMMARY`·`BOOK_INTRODUCTION_URL` 빈값 |
| **알라딘 프리미엄 신청** | **하지 않기로 결정(8/10)** — 아래 참고 |

### 알라딘 프리미엄을 안 하는 이유
신청서 조건 중 두 개를 못 지킨다.
1. **"타 인터넷서점 연결 없이 알라딘만 단독 연결"** — 북스타는 교보 전자책·YES24 크레마
   링크가 서비스의 핵심이라 뺄 수 없다
2. **"도서관/공공기관 명의만, SI구축개발업체 명의 불가"** — 우리는 업체
3. 추가로 "알라딘 구매하기 버튼 노출" 필요 → 도서관 앱에 서점 구매 버튼은 부적절

실익은 6일 → 3.5일(2.5일 단축)뿐. **세명대 정식계약 후 도서관 명의로는 재검토 여지 있음.**
현재 기본형은 조건 준수 중(앱 하단에 "표지·별점 알라딘 · 대출·키워드 정보나루" 출처 표기 있음).

---

## 성능 — 더 줄일 수 있는지 이미 다 따져봤다

**달력 6일은 API 한도가 정한 것이라 못 줄인다.** (정보나루 3만 + 알라딘 5천 = 하루 3.5만)

DB 쓰기가 느린 원인도 규명 완료:
- 같은 UPDATE인데 **embedding 있는 행 449ms/건 vs 없는 행 13ms/건 = 34배**
- 범인은 `idx_tulip_emb_paper`(HNSW). description만 바꿔도 행 재작성 → 인덱스 갱신
- 1GB 마이크로 인스턴스라 이게 지배적
- **인덱스를 내리면 하루 3.8시간 → 7분**이 되지만 **달력은 6일 그대로**(API 한도 때문).
  재빌드에 인스턴스 임시 업그레이드가 필요하고 과금·검색품질 영향이 있어 **하지 않기로 결정**

조회는 이미 최적화됨: 8스레드 병렬(372ms → 51ms). 정보나루는 동시 24도 받아준다(16ms).

---

## 백필 끝나면 할 일

1. **표지 핫링크 감시 장치 추가** (사용자와 합의됨, 백필 완료 후)
   - 우리가 저장하는 건 이미지가 아니라 **알라딘·네이버 URL**이다. 정보나루도 이미지를 호스팅하지 않는다
   - 2026-08-10 실측: 레퍼러 없음/bookstar/엉뚱한 사이트 **전부 정상** = 아직 안 막힘
   - 자체 저장 시 **9.3GB**(19.5만 장 × 50KB) + 전송비 → 이용자 0인 지금은 불필요
   - 대신 **매일 표지 100장 샘플 검사 → 실패율 급등 시 알림**을 데일리에 추가 (비용 0)
2. **재임베딩 검토** — 지금 채워지는 줄거리는 검색(별이 AI)에 반영되지 않는다.
   임베딩이 이미 만들어져 있기 때문. 백필 끝날 무렵 전체 재임베딩 1회(약 $0.3) 권장

---

## 이번 세션에 고친 것 (커밋)

| 커밋 | 내용 |
|---|---|
| `c49ac6c` | 데이터 오염 2종 수리(표지 이중스킴 147·설명 엔티티 1,638) + 재발 코드가드 |
| `fe1ff66` | 표지 백필 15배 — ctrl 정렬이 성능을 죽이고 있었음(7.5%→무작위 50%), eBook 재조회 0/111 낭비 제거 |
| `49f24da` | 표지 없는 책 대체표지 시안 4종 |
| `9e71274` | **A안 활자 표지 8만 권 적용**(호출부 10곳). 검색결과가 까만 사각형만 뜨던 버그 동시 수리 |
| `50329f8` | 줄거리 백필 사각지대 16.2만 권 편입(표지만 있고 줄거리 없던 책을 아예 안 보고 있었음) |
| `0b20fd3` | 줄거리 백필 최신 발행 우선 |
| `3add544` | **정보나루 전환** (52일 → 8일) |
| `b5b64c8` | 대상 쿼리가 15분 넘게 안 끝나 데일리가 멈추던 문제 (union+정렬키 → 두 풀 분리, 2.3초) |
| `9cbfd7f` | 조회 8스레드 병렬 + 쓰기 values 조인 1문 |
| `91d9e2b` | 데일리 로그 `python -u` (버퍼링돼 진행 중인데도 멈춘 것처럼 보였다) |

### 스케줄 작업도 고쳤다
`3221225786`(=Ctrl+C 강제종료)로 죽어 있었다. 원인 설정 3개를 껐다.
- `StopOnIdleEnd` True→False (**PC를 만지면 작업이 죽는다**)
- `StopIfGoingOnBatteries`, `DisallowStartIfOnBatteries` True→False
- 추가: 실패 시 20분 간격 3회 재시도, 실행시간제한 72h→12h

---

## 이번에 얻은 교훈 (같은 실수 반복 금지)

- **배치 쿼리는 실전 limit으로 시간을 재라.** limit 200~300 테스트는 33,860에서 터지는 걸 못 잡는다
- **에러 문자열로 성패를 판정할 땐 실제 응답을 눈으로 먼저 봐라.** 정보나루 한도 메시지엔
  '제한·초과·한도'가 하나도 없어서(요청·등록뿐) 차단 271건을 "빈 응답"으로 오분류했다
- **비교용 정규화가 원본을 훼손하지 않는지 확인하라.** strip()이 책 제목의 `<다크 플레이스>`를
  태그로 보고 지워서 "알라딘이 더 짧다"는 잘못된 결론이 나올 뻔했다
- **로그가 안 찍힌다고 멈춘 게 아니다.** `>>` 리다이렉트 시 파이썬 stdout은 블록 버퍼링된다
- **대량 UPDATE는 `right(ctrl,1)` 10분할.** 4,435행도 통짜면 서버 statement timeout(57014)
- **성과 0건을 버그로 단정하기 전에 실패 표본을 직접 조회해 봐라.** 알라딘 0/4,200은 코드가 아니라
  풀 구성(외서) 탓이었다. 반대 방향 실수(fe1ff66의 eBook 재조회 0/111 낭비)와 짝이 되는 교훈 —
  **0%를 봤으면 "코드가 죽었나"와 "대상이 없나"를 둘 다 확인**해야 한다 (2026-08-10)
- **로그와 DB 카운트가 안 맞으면 죽은 실행분을 의심하라.** 오늘 DB에 찍힌 갱신 행이 로그보다
  많았는데, 원인은 03:30 실행이 `-u` 없이 돌다 중단돼 **버퍼에만 있던 출력이 날아가고 DB 쓰기는
  남은 것**이었다(표지 7,602·줄거리 6,213을 로그 없이 채워놨다). 로그가 곧 실적이 아니다
- 🔑**진행률 기준값은 배치가 안 도는 시각에 재라.** 인계 문서의 "07:30 기준 173,674"가 03:30 실행
  도중 값이어서, 그걸 기준 삼으니 오늘 성과가 실제(+16,033)의 3분의 1(+4,763)로 보였다.
  **구간 실적은 `updated_at` 필터로 세는 게 유일하게 안전하다**
- 🔑**서버 `now()`·`updated_at`은 UTC다.** KST로 구간을 자르려면 `-9시간`. `date '2026-08-10'`으로
  자르면 KST 09:00 이후만 잡혀 하루치가 통째로 사라진다
